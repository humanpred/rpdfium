#!/usr/bin/env sh
# pdfium R package — Windows cleanup.
set -eu
PKG_ROOT="$(cd "$(dirname "$0")" && pwd)"
rm -f  "$PKG_ROOT/src/symbols.rds" "$PKG_ROOT/src/"*.o "$PKG_ROOT/src/"*.dll
rm -rf "$PKG_ROOT/inst/include" "$PKG_ROOT/inst/lib" "$PKG_ROOT/inst/bin"
