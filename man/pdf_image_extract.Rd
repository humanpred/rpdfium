% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/images.R
\name{pdf_image_extract}
\alias{pdf_image_extract}
\title{Extract an embedded image to a file, picking a sensible format}
\usage{
pdf_image_extract(obj, path)
}
\arguments{
\item{obj}{A \code{pdfium_obj} of type \code{"image"}.}

\item{path}{Output file path. If the extension is supplied it's
ignored — the function appends \code{.jpg}, \code{.jp2}, or \code{.png}
according to the chosen format and returns the actual path
used.}
}
\value{
Invisibly returns the path written, with the chosen
extension applied. The output format can be retrieved by
inspecting the file extension on the returned string.
}
\description{
Convenience helper over \code{\link[=pdf_image_filters]{pdf_image_filters()}}, \code{\link[=pdf_image_data]{pdf_image_data()}},
and \code{\link[=pdf_image_bitmap]{pdf_image_bitmap()}}. Inspects the image's filter chain and
picks an on-disk format:
}
\details{
\itemize{
\item \code{DCTDecode} → write the raw embedded bytes as \code{.jpg}.
\item \code{JPXDecode} → write the raw embedded bytes as \code{.jp2}.
\item \code{CCITTFaxDecode} / \code{JBIG2Decode} / \code{RunLengthDecode} /
\code{FlateDecode} / \code{LZWDecode} / \code{ASCII85Decode} / \code{ASCIIHexDecode}
chains, or no filter → rasterize via \code{\link[=pdf_image_bitmap]{pdf_image_bitmap()}} and
write as a PNG using \code{png::writePNG()}. PNG round-trips the
alpha channel when present.
}

Mirrors pypdfium2's \code{PdfImage.extract()} convenience.
}
\seealso{
\code{\link[=pdf_image_data]{pdf_image_data()}} to get the raw bytes directly,
\code{\link[=pdf_image_bitmap]{pdf_image_bitmap()}} for the decoded pixel matrix.
}
