% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tier3_extras.R
\name{pdf_text_obj_at_char}
\alias{pdf_text_obj_at_char}
\title{Look up the text page-object owning a given char by direct accessor}
\usage{
pdf_text_obj_at_char(page, char_index, page_num = 1L)
}
\arguments{
\item{page}{A \code{pdfium_page} from \code{\link[=pdf_page_load]{pdf_page_load()}}, or a
\code{pdfium_doc}.}

\item{char_index}{One-based character index (matches
\code{pdf_text_chars()$char_index}).}

\item{page_num}{One-based page index. Only used when \code{page} is
a \code{pdfium_doc}. Ignored otherwise.}
}
\value{
A \code{pdfium_obj} of type \code{"text"}, or \code{NULL} when the
char has no associated page-object.
}
\description{
Wraps \code{FPDFText_GetTextObject}. Returns a \code{pdfium_obj} of type
\code{"text"} for the page-object that contains the character at the
given 1-based \code{char_index} on the page's text page (matching the
\code{char_index} column of \code{\link[=pdf_text_chars]{pdf_text_chars()}}). Returns \code{NULL} when
the char has no associated page-object (e.g. PDFium-synthesised
whitespace).
}
\details{
Functionally equivalent to chaining
\code{pdf_text_char_obj_index() -> pdf_page_objects()[[i]]}, but
short-circuited through PDFium's direct accessor so callers
don't have to enumerate every page-object.
}
\seealso{
\code{\link[=pdf_text_char_obj_index]{pdf_text_char_obj_index()}} for the index-only version,
\code{\link[=pdf_text_chars]{pdf_text_chars()}} for per-character readouts.
}
