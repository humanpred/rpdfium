% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/images.R
\name{pdf_image_icc_profile}
\alias{pdf_image_icc_profile}
\title{Decoded ICC color profile bytes for an embedded image}
\usage{
pdf_image_icc_profile(obj)
}
\arguments{
\item{obj}{A \code{pdfium_obj} of type \code{"image"}.}
}
\value{
A \code{raw} vector. Length zero when the image has no ICC
profile.
}
\description{
Returns the raw bytes of the ICC color profile attached to the
image's colour space, if any. Useful for callers that need to
reproduce the colour rendering exactly (e.g. when re-encoding the
image outside PDFium). Wraps
\code{FPDFImageObj_GetIccProfileDataDecoded}.
}
\details{
Most embedded images carry no ICC profile — they use a standard
colour space (\verb{/DeviceRGB}, \verb{/DeviceGray}, etc.). This function
returns \code{raw(0)} in that common case.
}
