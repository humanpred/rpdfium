% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/doc_extra.R
\name{pdf_file_id}
\alias{pdf_file_id}
\title{Read the document's file identifier from its trailer}
\usage{
pdf_file_id(doc, id_type = c("permanent", "changing"), password = NULL)
}
\arguments{
\item{doc}{A \code{pdfium_doc} from \code{\link[=pdf_open]{pdf_open()}}, or a character path.}

\item{id_type}{One of \code{"permanent"} (default) or \code{"changing"}.}

\item{password}{Optional password for encrypted PDFs when \code{doc}
is a path. Ignored when \code{doc} is already an open \code{pdfium_doc}.}
}
\value{
A raw vector. Zero-length when the document has no
\verb{/ID} entry.
}
\description{
Returns the raw bytes of the PDF trailer's \verb{/ID} entry. The
identifier is a two-element array \verb{[permanent, changing]}:
\code{permanent} is a hash that should stay constant across saves
of the same logical document; \code{changing} is updated each time
the file is rewritten. Use \code{id_type = "permanent"} (the
default) to track document identity, or \code{"changing"} to detect
that the file has been re-saved.
}
\details{
Wraps \code{FPDF_GetFileIdentifier}. The identifier is binary; PDF
writers conventionally produce 16-byte MD5 hashes but the
length is unspecified and PDFs from non-standard writers may
return any byte string (or none at all).
}
\examples{
fixture <- system.file("extdata", "fixtures", "shapes.pdf",
  package = "pdfium"
)
if (nzchar(fixture)) pdf_file_id(fixture)
}
