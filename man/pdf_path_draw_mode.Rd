% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/paths.R
\name{pdf_path_draw_mode}
\alias{pdf_path_draw_mode}
\title{Path draw mode (fill rule + stroke flag)}
\usage{
pdf_path_draw_mode(obj)
}
\arguments{
\item{obj}{A \code{pdfium_obj} of type \code{"path"}.}
}
\value{
A named list with two elements:
\itemize{
\item \code{fill_mode} - one of \code{"none"}, \code{"even_odd"}, \code{"winding"}.
\code{NA} if PDFium reports no draw-mode (rare; typically only on
malformed paths).
\item \code{fill_mode_code} - the raw integer code (\code{0}, \code{1}, \code{2}) for
round-tripping with v0.2.0 writers.
\item \code{stroke} - logical scalar; \code{TRUE} if the path is stroked.
}
}
\description{
Returns whether a path object is stroked and which fill mode is
applied. A path can be:
}
\details{
\itemize{
\item Stroked only (\code{stroke = TRUE}, \code{fill_mode = "none"}).
\item Filled with non-zero winding rule (\code{fill_mode = "winding"}).
\item Filled with even-odd rule (\code{fill_mode = "even_odd"}).
\item Both stroked and filled (\code{stroke = TRUE} + \code{fill_mode != "none"}).
\item Invisible (\code{stroke = FALSE}, \code{fill_mode = "none"}) — used for
clip-only paths.
}

Wraps \code{FPDFPath_GetDrawMode}.
}
\seealso{
\code{\link[=pdf_path_stroke]{pdf_path_stroke()}} for the stroke color/width;
\code{\link[=pdf_path_fill]{pdf_path_fill()}} for the fill color.
}
