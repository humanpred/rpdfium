% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/path_setters.R
\name{pdf_path_append}
\alias{pdf_path_append}
\title{Append a sequence of path segments in one call}
\usage{
pdf_path_append(obj, segments)
}
\arguments{
\item{obj}{A \code{pdfium_obj} of type \code{"path"}. Parent doc must be
readwrite.}

\item{segments}{A tibble with at minimum the columns
\code{segment_type} (character), \code{x}, \code{y} (numeric), and optionally
\code{close_figure} (logical). Matches the \code{\link[=pdf_path_segments]{pdf_path_segments()}}
output exactly so a reader → edit → writer round-trip is a
one-liner.}
}
\value{
Invisibly returns the parent \code{pdfium_doc}.
}
\description{
Convenience wrapper that takes a tibble in the shape
\code{\link[=pdf_path_segments]{pdf_path_segments()}} returns and replays it as a series of
appender calls on \code{obj}. Useful when you've read a path with
\code{\link[=pdf_path_segments]{pdf_path_segments()}}, edited the rows in R, and want to append
the modified geometry to a fresh path object.
}
\details{
Segment dispatch by the \code{segment_type} column:
\itemize{
\item \code{"moveto"} → \code{\link[=pdf_path_move_to]{pdf_path_move_to()}} with \verb{(x, y)}.
\item \code{"lineto"} → \code{\link[=pdf_path_line_to]{pdf_path_line_to()}} with \verb{(x, y)}.
\item \code{"bezierto"} → cubic Bezier. PDFium's reader surfaces each
cubic curve as \strong{three} consecutive \code{bezierto} rows (two
control points then the endpoint); this wrapper buffers two
rows and emits a single \code{\link[=pdf_path_bezier_to]{pdf_path_bezier_to()}} call on the
third.
}

Any row whose \code{close_figure} column is \code{TRUE} triggers a
\code{\link[=pdf_path_close]{pdf_path_close()}} after its segment.
}
\seealso{
\code{\link[=pdf_path_segments]{pdf_path_segments()}}.
}
