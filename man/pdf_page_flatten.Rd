% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/form_field_setters.R
\name{pdf_page_flatten}
\alias{pdf_page_flatten}
\title{Flatten form fields and annotations into the page content stream}
\usage{
pdf_page_flatten(page, mode = c("display", "print"))
}
\arguments{
\item{page}{A \code{pdfium_page} from \code{\link[=pdf_page_load]{pdf_page_load()}}. Parent doc
must be readwrite.}

\item{mode}{Character scalar; one of \code{"display"} or \code{"print"}.}
}
\value{
Invisibly returns the parent \code{pdfium_doc}.
}
\description{
Wraps \code{FPDFPage_Flatten}. After flattening, form widgets and
annotations are baked into the page's content stream as static
graphics — they no longer exist as interactive objects. \strong{This
is irreversible}: there is no \code{pdf_page_unflatten()}. Use this
before saving a final-state PDF that downstream consumers must
not edit.
}
\details{
Two modes:
\itemize{
\item \code{"display"} (default) — bake the on-screen appearance of every
annot / widget.
\item \code{"print"} — bake the print-time appearance instead.
}

Returns the page invisibly. The parent page's dirty mark is set
so \code{\link[=pdf_save]{pdf_save()}} picks up the change.
}
\seealso{
\code{\link[=pdf_save]{pdf_save()}}.
}
