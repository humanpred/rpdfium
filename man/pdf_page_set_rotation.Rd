% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mutation.R
\name{pdf_page_set_rotation}
\alias{pdf_page_set_rotation}
\title{Set a page's rotation}
\usage{
pdf_page_set_rotation(page, degrees, page_num = 1L)
}
\arguments{
\item{page}{A \code{pdfium_page} from \code{\link[=pdf_page_load]{pdf_page_load()}}, or a
\code{pdfium_doc} (in which case \code{page_num} selects the page).}

\item{degrees}{Integer; one of \code{0}, \code{90}, \code{180}, \code{270}.}

\item{page_num}{One-based page index. Only used when \code{page} is a
\code{pdfium_doc}. Ignored otherwise.}
}
\value{
Invisibly returns the parent \code{pdfium_doc} so calls can
be chained with \verb{|>}.
}
\description{
Wraps \code{FPDFPage_SetRotation}. Allowed values are \code{0}, \code{90},
\code{180}, \code{270} degrees (clockwise). The PDF spec restricts page
rotation to multiples of 90; PDFium silently treats any other
value as 0.
}
\details{
Polymorphic in \code{page}: accepts either an already-loaded
\code{pdfium_page} from \code{\link[=pdf_page_load]{pdf_page_load()}} (with \code{readwrite = TRUE}
on the parent doc) or a \code{pdfium_doc} plus \code{page_num}.
}
\seealso{
\code{\link[=pdf_page_rotation]{pdf_page_rotation()}} for the read side.
}
