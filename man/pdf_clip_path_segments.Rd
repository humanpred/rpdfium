% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clip.R
\name{pdf_clip_path_segments}
\alias{pdf_clip_path_segments}
\title{Read all segments of a clip path as a tibble}
\usage{
pdf_clip_path_segments(clip_path)
}
\arguments{
\item{clip_path}{A \code{pdfium_clip_path} from \code{\link[=pdf_obj_clip_path]{pdf_obj_clip_path()}}.}
}
\value{
A tibble with columns:
\itemize{
\item \code{path_index} integer - 1-based sub-path index within the clip
\item \code{seg_index} integer - 1-based segment index within its sub-path
\item \code{seg_type} character - \code{"moveto"}, \code{"lineto"}, \code{"bezierto"},
or \code{"unknown"}
\item \code{x}, \code{y} numeric - segment coordinates in PDF user space
\item \code{close_figure} logical - whether this segment closes its
sub-path
}
}
\description{
Returns a data frame describing every segment in every sub-path
of \code{clip_path}, ordered first by \code{path_index} and then by
\code{seg_index} within each sub-path. Mirrors the shape of
\code{\link[=pdf_path_segments]{pdf_path_segments()}} but adds a \code{path_index} column for the
clip's outer level. Wraps \code{FPDFClipPath_CountPaths},
\code{FPDFClipPath_CountPathSegments}, and
\code{FPDFClipPath_GetPathSegment}.
}
\details{
Coordinates are in PDF user space (typically points, with the
origin at the page's bottom-left).
}
\seealso{
\code{\link[=pdf_path_segments]{pdf_path_segments()}} for the same shape applied to a
regular page object's path.
}
