% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract.R
\name{pdf_extract_paths}
\alias{pdf_extract_paths}
\title{Extract all path geometry on a page into a single tibble}
\usage{
pdf_extract_paths(path, page = 1L)
}
\arguments{
\item{path}{Either a character scalar path to a PDF file, or an
already-open \code{pdfium_doc} returned by \code{\link[=pdf_open]{pdf_open()}}. When \code{path}
is a character path the document is opened and closed internally.}

\item{page}{One-based page index (default \code{1}).}
}
\value{
A tibble with the schema described above.
}
\description{
One-call helper that opens a document (or accepts an already-open
one), enumerates every path object on the requested page, and
returns a tibble with one row per path segment carrying both the
geometry and the containing path's stroke / fill style and
bounding box. This is the function \code{kmextract} consumes via the
\code{pdfium_native} backend.
}
\details{
\subsection{Returned tibble}{

Each row describes one path-segment operator (a \code{moveto},
\code{lineto}, or \code{bezierto}), in the order PDFium emits them:

Path identity & segment geometry:
\itemize{
\item \code{path_index} - 1-based index of the parent path object on the page
\item \code{segment_index} - 1-based segment index within the path
\item \code{type} - \code{"moveto"}, \code{"lineto"}, \code{"bezierto"}, or \code{"unknown"}
\item \code{x}, \code{y} - the segment's anchor / endpoint in PDF points
\item \code{close} - logical, segment closes the current subpath
}

Style (constant across all rows of one path):
\itemize{
\item \code{stroke_red}, \code{stroke_green}, \code{stroke_blue}, \code{stroke_alpha} -
0-255 channels; \code{NA} if no stroke
\item \code{stroke_width} - PDF points; \code{NA} if no stroke
\item \code{fill_red}, \code{fill_green}, \code{fill_blue}, \code{fill_alpha} -
0-255 channels; \code{NA} if no fill
}

Path bounding box (constant across rows of one path):
\itemize{
\item \code{bounds_left}, \code{bounds_bottom}, \code{bounds_right}, \code{bounds_top} -
PDF points
}
}

\subsection{Attributes}{
\itemize{
\item \code{page_size_pt} - named numeric \code{c(width, height)} of the page,
from \code{\link[=pdf_page_size]{pdf_page_size()}}
\item \code{page_rotation} - integer in \verb{\{0, 90, 180, 270\}}, from
\code{\link[=pdf_page_rotation]{pdf_page_rotation()}}
\item \code{text_runs} - tibble with one row per text object on the page:
\code{text_index}, bounds, \code{font_size}, and \code{text} (UTF-8 content
extracted via \code{\link[=pdf_text_content]{pdf_text_content()}}).
}
}

\subsection{Known limitations}{
\itemize{
\item Bezier control points are not exposed - only segment endpoints.
PDFium does not expose them through its public C API; see
\code{dev/decisions/ADR-009-defer-bezier-controls.md}.
}
}
}
\examples{
fixture <- system.file("extdata", "fixtures", "shapes.pdf",
                       package = "pdfium")
if (nzchar(fixture)) {
  paths <- pdf_extract_paths(fixture, page = 1)
  head(paths)
  attr(paths, "page_size_pt")
  attr(paths, "text_runs")
}
}
\seealso{
\code{\link[=pdf_path_segments]{pdf_path_segments()}}, \code{\link[=pdf_path_stroke]{pdf_path_stroke()}},
\code{\link[=pdf_path_fill]{pdf_path_fill()}}, \code{\link[=pdf_obj_bounds]{pdf_obj_bounds()}}
}
