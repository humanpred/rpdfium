% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/form_fields.R
\name{as_tibble.pdfium_form_field_list}
\alias{as_tibble.pdfium_form_field_list}
\title{Tibble view of a \code{pdfium_form_field_list}}
\usage{
\method{as_tibble}{pdfium_form_field_list}(x, ...)
}
\arguments{
\item{x}{A \code{pdfium_form_field_list} from \code{\link[=pdf_form_fields]{pdf_form_fields()}}.}

\item{...}{Unused (S3 generic compatibility).}
}
\value{
A tibble matching the previous \code{pdf_form_fields()}
shape plus \code{handle} + \code{source} columns.
}
\description{
Walks the list of field handles and reads every documented
AcroForm property into a wide tibble. Adds two list-columns
relative to a simple data extraction: \code{handle} (the
\code{pdfium_form_field} per row) and \code{source} (the parent
\code{pdfium_doc}).
}
\details{
Internally calls the existing bulk reader (\code{cpp_form_fields_list})
for speed; per-row handles are pulled from the list itself so
R-object identity survives round-trip.
}
