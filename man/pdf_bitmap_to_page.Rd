% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/render.R
\name{pdf_bitmap_to_page}
\alias{pdf_bitmap_to_page}
\title{Convert bitmap pixel coordinates to PDF page-space points}
\usage{
pdf_bitmap_to_page(bitmap, x, y)
}
\arguments{
\item{bitmap}{A \code{pdfium_bitmap} from \code{\link[=pdf_render_page]{pdf_render_page()}}. Must
carry render-geometry metadata (older \code{pdf_render_page_with_matrix()}
output doesn't).}

\item{x, y}{Numeric vectors of pixel positions on the bitmap.
Recycled to a common length.}
}
\value{
A two-column tibble with columns \code{x} and \code{y} in PDF
user-space points (origin at the page's bottom-left).
}
\description{
Inverse of \code{\link[=pdf_bitmap_from_page]{pdf_bitmap_from_page()}}. Translates pixel positions
in a \code{pdfium_bitmap} (returned by \code{\link[=pdf_render_page]{pdf_render_page()}}) to PDF
user-space points on the source page. Wraps \code{FPDF_DeviceToPage}
with the render-geometry that produced the bitmap pre-populated
from the bitmap's attributes.
}
\examples{
fixture <- system.file("extdata", "fixtures", "shapes.pdf",
  package = "pdfium"
)
if (nzchar(fixture)) {
  doc <- pdf_doc_open(fixture)
  bmp <- pdf_render_page(doc, dpi = 72)
  # Map two bitmap pixel positions back to PDF page-space points.
  pdf_bitmap_to_page(bmp, x = c(0, 100), y = c(0, 100))
  pdf_doc_close(doc)
}
}
\seealso{
\code{\link[=pdf_bitmap_from_page]{pdf_bitmap_from_page()}} for the inverse,
\code{\link[=pdf_device_to_page]{pdf_device_to_page()}} for the underlying primitive.
}
