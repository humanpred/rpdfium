% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annot_authoring.R
\name{pdf_annot_new}
\alias{pdf_annot_new}
\title{Create a new annotation on a page}
\usage{
pdf_annot_new(page, subtype, bounds = NULL)
}
\arguments{
\item{page}{A \code{pdfium_page} from \code{\link[=pdf_page_load]{pdf_page_load()}}. Parent doc
must be readwrite.}

\item{subtype}{Character scalar — one of the supported annotation
subtypes listed above.}

\item{bounds}{Optional length-4 numeric vector
\code{c(left, bottom, right, top)} in PDF user-space points. Default
\code{NULL} (annotation has no rect set — most subtypes still need
one and you'll likely follow up with
\code{\link[=pdf_annot_set_bounds]{pdf_annot_set_bounds()}}).}
}
\value{
The new \code{pdfium_annot} handle.
}
\description{
Wraps \code{FPDFPage_CreateAnnot} + (optionally) \code{FPDFAnnot_SetRect}.
PDFium supports creating annotations of subtype \code{"circle"},
\code{"fileattachment"}, \code{"freetext"}, \code{"highlight"}, \code{"ink"},
\code{"link"}, \code{"popup"}, \code{"square"}, \code{"squiggly"}, \code{"stamp"},
\code{"strikeout"}, \code{"text"}, and \code{"underline"}. Other subtypes
(\code{"widget"}, \code{"polygon"}, \code{"line"}, etc.) error from PDFium.
}
\details{
The new annotation is appended to the page's \verb{/Annots} array.
Use \code{\link[=pdf_annotations]{pdf_annotations()}} to re-read the page if you need an
updated handle list — the new annotation lands at the end.
}
\seealso{
\code{\link[=pdf_annot_delete]{pdf_annot_delete()}}, \code{\link[=pdf_annot_set_bounds]{pdf_annot_set_bounds()}},
\code{\link[=pdf_annot_set_color]{pdf_annot_set_color()}}, \code{\link[=pdf_annot_set_contents]{pdf_annot_set_contents()}}.
}
