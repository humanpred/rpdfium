% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/page_extras.R
\name{pdf_text_index_from_char}
\alias{pdf_text_index_from_char}
\alias{pdf_text_char_from_text_index}
\title{Map between PDFium's "all characters" and "extractable text" indices}
\usage{
pdf_text_index_from_char(page, char_index, page_num = 1L)

pdf_text_char_from_text_index(page, text_index, page_num = 1L)
}
\arguments{
\item{page}{A \code{pdfium_page} from \code{\link[=pdf_page_load]{pdf_page_load()}}, or a
\code{pdfium_doc}.}

\item{char_index}{One-based character index (matches
\code{pdf_text_chars()$char_index}).}

\item{page_num}{One-based page index. Only used when \code{page} is
a \code{pdfium_doc}. Ignored otherwise.}

\item{text_index}{Zero-based offset into the extractable text
string.}
}
\value{
An integer scalar — the converted index, or \code{NA} when the
character has no counterpart in the other indexing system.
}
\description{
PDFium's text page surfaces two parallel views of the page's text:
the full \emph{character} list (positioned glyphs including
PDFium-synthesised whitespace between them), and the \emph{extractable
text} string (only characters that appear in \code{\link[=pdf_doc_text]{pdf_doc_text()}}'s
output). These helpers translate between the two indexing systems.
}
\details{
\code{pdf_text_index_from_char()} converts a 1-based \code{char_index}
(matches \code{pdf_text_chars()}'s \code{char_index} column) into the
0-based position in the extractable text string, or \code{NA} if the
character has no extractable-text counterpart.

\code{pdf_text_char_from_text_index()} does the reverse: given a
0-based text-string index, returns the 1-based \code{char_index}.

Wraps \code{FPDFText_GetTextIndexFromCharIndex} /
\code{FPDFText_GetCharIndexFromTextIndex}.
}
\seealso{
\code{\link[=pdf_text_chars]{pdf_text_chars()}}, \code{\link[=pdf_doc_text]{pdf_doc_text()}}, \code{\link[=pdf_text_search]{pdf_text_search()}}.
}
