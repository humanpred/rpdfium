% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/render.R
\name{pdf_render_page}
\alias{pdf_render_page}
\title{Render a PDF page to a bitmap}
\usage{
pdf_render_page(
  page,
  page_num = 1L,
  dpi = 72,
  background = "white",
  annotations = FALSE,
  rotation = 0L
)
}
\arguments{
\item{page}{A \code{pdfium_page} from \code{\link[=pdf_load_page]{pdf_load_page()}}, or a
\code{pdfium_doc} (the page given by \code{page_num} will be loaded and
closed internally).}

\item{page_num}{One-based page index. Only used when \code{page} is a
\code{pdfium_doc}. Ignored otherwise.}

\item{dpi}{Render resolution in dots per inch (default \code{72},
meaning one pixel per PDF point). Higher values give larger,
sharper output at proportional memory cost.}

\item{background}{Background color drawn behind the page content
before rendering. Accepts any string \code{\link[grDevices:col2rgb]{grDevices::col2rgb()}}
understands (named color, \code{"#RRGGBB"}, \code{"#RRGGBBAA"}), or \code{NA}
for a fully transparent background. Defaults to \code{"white"}.}

\item{annotations}{Logical; render annotation appearance streams
on top of the page content. Defaults to \code{FALSE}.}

\item{rotation}{Extra rotation in degrees applied on top of the
page's own \verb{/Rotate} attribute. One of \code{0}, \code{90}, \code{180}, \code{270}.
Note: PDFium's rotation is clockwise; e.g. \code{90} means rotate
the page 90° clockwise from its on-page orientation.}
}
\value{
A \code{pdfium_bitmap} object - an integer matrix with \code{class = c("pdfium_bitmap", "nativeRaster")}, \code{dim = c(height, width)},
\code{channels = 4L}, plus attributes \code{dpi}, \code{source_page},
\code{rotation_applied}.
}
\description{
Rasterises one page of a PDF document via PDFium and returns a
\code{pdfium_bitmap} object (an integer matrix that inherits from base
R's \code{nativeRaster} class). Use \code{\link[graphics:plot]{graphics::plot()}} for an
immediate-display path (the S3 method here routes through
\code{\link[grid:grid.raster]{grid::grid.raster()}} on a 3-D RGBA array, the one R-engine
combination that renders pixel-for-pixel correctly across
platforms). Conversion helpers (\code{\link[=as.raster.pdfium_bitmap]{as.raster.pdfium_bitmap()}},
\code{\link[=as.array.pdfium_bitmap]{as.array.pdfium_bitmap()}}, \code{\link[=as.matrix.pdfium_bitmap]{as.matrix.pdfium_bitmap()}}) cover
the other common bitmap shapes downstream packages expect.
}
\examples{
fixture <- system.file("extdata", "fixtures", "shapes.pdf",
                       package = "pdfium")
if (nzchar(fixture)) {
  bmp <- pdf_render_page(pdf_open(fixture), dpi = 96)
  bmp                                # human summary
  if (interactive()) plot(bmp)       # render to the active device
}
}
\seealso{
\code{\link[=as.raster.pdfium_bitmap]{as.raster.pdfium_bitmap()}},
\code{\link[=as.array.pdfium_bitmap]{as.array.pdfium_bitmap()}} for output-shape conversions;
\code{\link[=pdf_page_size]{pdf_page_size()}} and \code{\link[=pdf_page_rotation]{pdf_page_rotation()}} for the source
page's dimensions.
}
