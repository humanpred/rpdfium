% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/doc.R
\name{pdf_page_label}
\alias{pdf_page_label}
\title{Read the logical page label of a PDF page}
\usage{
pdf_page_label(doc, page_num = 1L)
}
\arguments{
\item{doc}{A \code{pdfium_doc} from \code{\link[=pdf_open]{pdf_open()}}, or a character path.}

\item{page_num}{One-based physical page index (default \code{1}).}
}
\value{
Character scalar - the page's logical label, UTF-8
encoded. Empty string when the PDF doesn't carry a labels
table for this page (PDFium falls back to the physical
number's string form in some cases, but the contract is "may
be empty").
}
\description{
PDFs distinguish "physical" page numbers (1, 2, 3, ...) from
"logical" labels (e.g. "i", "ii", "iii" for front-matter then
"1", "2", "3" for the body, or "A-1", "A-2" for an appendix).
Wraps \code{FPDF_GetPageLabel}.
}
\seealso{
\code{\link[=pdf_page_labels]{pdf_page_labels()}} for every page's label at once,
\code{\link[=pdf_bookmarks]{pdf_bookmarks()}}.
}
