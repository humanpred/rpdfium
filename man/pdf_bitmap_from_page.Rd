% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/render.R
\name{pdf_bitmap_from_page}
\alias{pdf_bitmap_from_page}
\title{Convert PDF page-space points to bitmap pixel coordinates}
\usage{
pdf_bitmap_from_page(bitmap, x, y)
}
\arguments{
\item{bitmap}{A \code{pdfium_bitmap} from \code{\link[=pdf_render_page]{pdf_render_page()}}.}

\item{x, y}{Numeric vectors of PDF user-space coordinates.
Recycled to a common length.}
}
\value{
A two-column tibble with integer columns \code{x} and \code{y} in
bitmap pixel coordinates (origin at the bitmap's top-left).
}
\description{
Inverse of \code{\link[=pdf_bitmap_to_page]{pdf_bitmap_to_page()}}. Translates PDF user-space
points to pixel positions on a \code{pdfium_bitmap} (returned by
\code{\link[=pdf_render_page]{pdf_render_page()}}). Wraps \code{FPDF_PageToDevice} with the
render-geometry that produced the bitmap pre-populated from the
bitmap's attributes.
}
\seealso{
\code{\link[=pdf_bitmap_to_page]{pdf_bitmap_to_page()}} for the inverse,
\code{\link[=pdf_page_to_device]{pdf_page_to_device()}} for the underlying primitive.
}
