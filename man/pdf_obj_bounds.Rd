% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/objects.R
\name{pdf_obj_bounds}
\alias{pdf_obj_bounds}
\title{Axis-aligned bounding box of a page object}
\usage{
pdf_obj_bounds(obj)
}
\arguments{
\item{obj}{A \code{pdfium_obj} from \code{\link[=pdf_page_objects]{pdf_page_objects()}}.}
}
\value{
A named numeric vector with elements \code{left}, \code{bottom},
\code{right}, \code{top}. Width is \code{right - left}, height is \code{top - bottom}.
}
\description{
Returns the smallest rectangle, in PDF point coordinates, that
contains all visible parts of \code{obj}. The bounds are in the page's
own coordinate system, i.e. origin at the bottom-left of the
un-rotated media box (matching \code{pdf_page_size()}). Note that the
bounds are not adjusted for the page's rotation; consult
\code{\link[=pdf_page_rotation]{pdf_page_rotation()}} when comparing positions across rotated
pages.
}
\examples{
fixture <- system.file("extdata", "fixtures", "shapes.pdf",
  package = "pdfium"
)
if (nzchar(fixture)) {
  doc <- pdf_doc_open(fixture)
  p <- pdf_page_load(doc, 1)
  objs <- pdf_page_objects(p)
  pdf_obj_bounds(objs[[1]])
  pdf_page_close(p)
  pdf_doc_close(doc)
}
}
