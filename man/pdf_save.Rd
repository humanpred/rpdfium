% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/save.R
\name{pdf_save}
\alias{pdf_save}
\title{Save a PDF document to disk}
\usage{
pdf_save(
  doc,
  file,
  incremental = FALSE,
  remove_security = FALSE,
  subset_new_fonts = TRUE,
  version = NULL
)
}
\arguments{
\item{doc}{A \code{pdfium_doc} from \code{\link[=pdf_doc_open]{pdf_doc_open()}} or \code{\link[=pdf_doc_new]{pdf_doc_new()}}.}

\item{file}{Destination path. The directory must exist.}

\item{incremental}{Logical. If \code{TRUE}, append an incremental
update preserving the original byte layout (required for
signed-PDF workflows). If \code{FALSE} (default), rewrite the
whole file.}

\item{remove_security}{Logical. If \code{TRUE}, strip the
encryption dictionary from the saved copy. Defaults \code{FALSE}.
Use with caution.}

\item{subset_new_fonts}{Logical. If \code{TRUE} (default), subset
newly-embedded fonts the same way Acrobat does. Set \code{FALSE}
to embed full font tables.}

\item{version}{Integer or \code{NULL}. The PDF version in PDFium's
"10 * major + minor" form (e.g. \code{17} for PDF 1.7). \code{NULL}
(default) preserves the input file's declared version.}
}
\value{
Invisibly returns \code{file}, the path written to.
}
\description{
Serialises an in-memory \code{pdfium_doc} (typically produced by
\code{\link[=pdf_doc_open]{pdf_doc_open()}} with \code{readwrite = TRUE} and one or more mutators)
to a file. Wraps \code{FPDF_SaveAsCopy} and \code{FPDF_SaveWithVersion}.
}
\details{
\code{pdf_save()} writes atomically: PDFium's bytes go into a
tempfile in the destination directory, and on success the
tempfile is renamed over \code{file}. If the save fails mid-write,
the original \code{file} (if any) is preserved untouched.

Works on read-only documents too — opening a PDF, calling
\code{pdf_save()}, and re-opening the result is a way to "normalise"
a PDF (rebuild the xref table, etc.) without modifying its
content.
}
\seealso{
\code{\link[=pdf_save_to_raw]{pdf_save_to_raw()}} for in-memory output;
\code{\link[=pdf_doc_open]{pdf_doc_open()}} for the read side; \code{\link[=pdf_doc_new]{pdf_doc_new()}} for a fresh
document.
}
