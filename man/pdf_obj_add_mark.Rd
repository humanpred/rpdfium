% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/obj_setters.R
\name{pdf_obj_add_mark}
\alias{pdf_obj_add_mark}
\title{Add a content mark to a page object}
\usage{
pdf_obj_add_mark(obj, name, params = list())
}
\arguments{
\item{obj}{A \code{pdfium_obj} from \code{\link[=pdf_page_objects]{pdf_page_objects()}}. Parent doc
must be readwrite.}

\item{name}{Character scalar — the mark's name (e.g. \code{"Span"},
\code{"Artifact"}, \code{"MCID"}).}

\item{params}{Optional named list of integer- or character-typed
parameter values to attach to the mark. Numeric values are
coerced to integer; character values are written as strings.
Other types raise an error.}
}
\value{
Invisibly returns the parent \code{pdfium_doc}.
}
\description{
Wraps \code{FPDFPageObj_AddMark}. Content marks tag the object for
downstream consumers (the structure tree, custom workflows).
Optional \code{params} are written via \code{FPDFPageObjMark_SetIntParam}
or \verb{_SetStringParam} depending on each value's R type.
}
\seealso{
\code{\link[=pdf_obj_marks]{pdf_obj_marks()}}, \code{\link[=pdf_obj_remove_mark]{pdf_obj_remove_mark()}}.
}
