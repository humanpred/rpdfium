% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annot_authoring.R
\name{pdf_annot_set_color}
\alias{pdf_annot_set_color}
\title{Set the stroke / line color of an annotation}
\usage{
pdf_annot_set_color(
  annot,
  color = NULL,
  red = NULL,
  green = NULL,
  blue = NULL,
  alpha = NULL
)
}
\arguments{
\item{annot}{A \code{pdfium_annot} handle. Parent doc must be
readwrite.}

\item{color}{Length-3 (RGB) or length-4 (RGBA) numeric vector,
or \code{NULL} to keep the current color and rely on the
per-channel overrides.}

\item{red, green, blue, alpha}{Individual channel overrides.}
}
\value{
Invisibly returns the parent \code{pdfium_doc}.
}
\description{
Wraps \code{FPDFAnnot_SetColor} with \code{type = FPDFANNOT_COLORTYPE_Color}.
Composite setter — pass \code{color = c(r, g, b)} (or \code{c(r, g, b, a)})
for a full replacement, or individual \code{red} / \code{green} / \code{blue} /
\code{alpha} arguments for a partial overlay on the current color.
0-255 ints and 0-1 doubles are auto-detected per ADR-018 §5.
}
\seealso{
\code{\link[=pdf_annot_color]{pdf_annot_color()}},
\code{\link[=pdf_annot_set_interior_color]{pdf_annot_set_interior_color()}}.
}
