% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/page_nav.R
\name{pdf_page_actions}
\alias{pdf_page_actions}
\title{Page additional actions (open / close handlers)}
\usage{
pdf_page_actions(page, page_num = 1L)
}
\arguments{
\item{page}{A \code{pdfium_page} from \code{\link[=pdf_page_load]{pdf_page_load()}}, or a
\code{pdfium_doc} (the page given by \code{page_num} will be loaded
and closed internally).}

\item{page_num}{One-based page index. Only used when \code{page} is a
\code{pdfium_doc}. Ignored otherwise.}
}
\value{
A tibble with one row per defined additional-action.
Columns:
\itemize{
\item \code{trigger} — \code{"open"} or \code{"close"}.
\item \code{action_type} — same vocabulary as
\code{\link[=pdf_link_at_point]{pdf_link_at_point()}}'s \code{action_type}.
\item \code{uri}, \code{filepath}, \code{dest_page} — payload columns, same shape
as in \code{pdf_link_at_point()}.
}
}
\description{
PDF pages can declare actions that fire when the page is opened
(\verb{/AA/O}) or closed (\verb{/AA/C}) — for example, to play a sound, run
JavaScript, or follow a URI. \code{pdf_page_actions()} enumerates
those actions for one page. Wraps \code{FPDF_GetPageAAction} plus the
\verb{FPDFAction_*} accessors.
}
\details{
Most PDFs don't declare page additional-actions; the typical
result is an empty tibble.
}
