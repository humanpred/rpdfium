% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mutation.R
\name{pdf_reorder_pages}
\alias{pdf_reorder_pages}
\title{Reorder pages}
\usage{
pdf_reorder_pages(doc, new_order = NULL, move_pages = NULL, dest = NULL)
}
\arguments{
\item{doc}{A read-write \code{pdfium_doc} from
\code{\link[=pdf_open]{pdf_open()}} (with \code{readwrite = TRUE}) or \code{\link[=pdf_new_doc]{pdf_new_doc()}}.}

\item{new_order}{Integer vector. Either (a) a full permutation
of \code{1:pdf_page_count(doc)} — the document is reordered in
place to that permutation — or (b) the contiguous-move case
handled via \code{move_pages} + \code{dest}.}

\item{move_pages}{Integer vector of 1-based source page
indices to move (ignored when \code{new_order} is a full
permutation).}

\item{dest}{One-based destination index for the moved pages
(ignored when \code{new_order} is a full permutation).}
}
\value{
Invisibly returns \code{doc}.
}
\description{
Wraps \code{FPDF_MovePages}. Moves a contiguous set of pages to a
new position within the document. The most common use is
rearranging a full document to a new order; pass a permutation
of \code{seq_len(pdf_page_count(doc))} as \code{new_order}.
}
\details{
For partial reorderings (move pages 3–5 to position 1), pass
the source indices and the insertion point separately.
}
