% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/signatures.R
\name{pdf_signatures}
\alias{pdf_signatures}
\title{List the digital signatures attached to a PDF document}
\usage{
pdf_signatures(doc)
}
\arguments{
\item{doc}{A \code{pdfium_doc} from \code{\link[=pdf_open]{pdf_open()}}, or a character path.}
}
\value{
A tibble with columns:
\itemize{
\item \code{signature_index} integer - 1-based; pass to
\code{\link[=pdf_signature_contents]{pdf_signature_contents()}} / \code{\link[=pdf_signature_byte_range]{pdf_signature_byte_range()}}.
\item \code{sub_filter} character - the signature's \verb{/SubFilter}
value, e.g. \code{"adbe.pkcs7.detached"}, \code{"ETSI.CAdES.detached"},
\code{"adbe.x509.rsa_sha1"}. ASCII.
\item \code{reason} character - UTF-8 reason / comment string,
attached when the signer wrote one. Empty if absent.
\item \code{time} character - signing time in PDF date format
(\code{"D:YYYYMMDDHHmmSS+HH'mm'"}). Empty if the signature
defers to the timestamp inside the PKCS#7 blob. Pass to
\code{\link[=pdf_parse_date]{pdf_parse_date()}} for a POSIXct.
\item \code{doc_mdp_permission} integer - 1, 2, or 3 (PDF DocMDP
permission level: no changes / form-fill only / form-fill
\itemize{
\item annotations + signing fields). \code{NA} when no DocMDP entry
is present.
}
\item \code{contents_size} integer - byte length of the signature
blob (DER-encoded PKCS#1 or PKCS#7).
\item \code{byte_range_pairs} integer - number of (offset, length)
pairs covered by the signed digest. Pass \code{signature_index}
to \code{\link[=pdf_signature_byte_range]{pdf_signature_byte_range()}} for the actual pairs.
}

Returns a 0-row tibble of the same schema when the document
has no signatures.
}
\description{
Returns a tibble row per signature object in the document.
Wraps \code{FPDF_GetSignatureCount}, \code{FPDF_GetSignatureObject}, and
the \verb{FPDFSignatureObj_*} scalar accessors.
}
\seealso{
\code{\link[=pdf_signature_contents]{pdf_signature_contents()}} for the raw PKCS#7 / PKCS#1
bytes, \code{\link[=pdf_signature_byte_range]{pdf_signature_byte_range()}} for the signed byte
ranges, \code{\link[=pdf_parse_date]{pdf_parse_date()}} for parsing the \code{time} column.
}
