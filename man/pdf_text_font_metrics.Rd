% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/glyph_paths.R
\name{pdf_text_font_metrics}
\alias{pdf_text_font_metrics}
\title{Font ascent and descent for a text page-object's font}
\usage{
pdf_text_font_metrics(obj, font_size = 1)
}
\arguments{
\item{obj}{A \code{pdfium_obj} of type \code{"text"}.}

\item{font_size}{Numeric font size in PDF points (default \code{1},
so the result is in "em" units — multiply by the actual font
size you care about).}
}
\value{
A named list with two numeric scalars: \code{ascent} and
\code{descent}. Either is \code{NA} when PDFium can't resolve it.
}
\description{
Returns the font's vertical metrics — \emph{ascent} (the maximum
height above the baseline) and \emph{descent} (the maximum depth
below the baseline, conventionally a negative number) — at the
requested \code{font_size}. Useful for laying out text with
consistent line heights and for converting between PDF text
coordinates (baseline-relative) and bounding-box coordinates.
}
\details{
Wraps \code{FPDFFont_GetAscent} and \code{FPDFFont_GetDescent}.
}
\seealso{
\code{\link[=pdf_text_font]{pdf_text_font()}} for the font's name + weight +
italic-angle metadata; \code{\link[=pdf_glyph_path]{pdf_glyph_path()}} for per-glyph
outlines.
}
