% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/images.R
\name{pdf_image_info}
\alias{pdf_image_info}
\title{Inspect metadata for an embedded image}
\usage{
pdf_image_info(obj)
}
\arguments{
\item{obj}{A \code{pdfium_obj} of type \code{"image"}, typically returned by
filtering \code{\link[=pdf_page_objects]{pdf_page_objects()}} on \code{type == "image"}.}
}
\value{
A named list with elements \code{width}, \code{height} (integer
pixels), \code{horizontal_dpi}, \code{vertical_dpi} (numeric, may be 0
when the image has no explicit DPI), \code{bits_per_pixel} (integer),
\code{colorspace} (character; one of
"Unknown", "DeviceGray", "DeviceRGB", "DeviceCMYK", "CalGray", "CalRGB", "Lab", "ICCBased", "Separation", "DeviceN", "Indexed", "Pattern"),
and \code{marked_content_id} (integer; \code{-1} when absent).
}
\description{
Reads dimensions, DPI, bits-per-pixel, and color space from a
\code{pdfium_obj} of type \code{"image"}. Wraps \code{FPDFImageObj_GetImageMetadata}
(plus \code{FPDFImageObj_GetImagePixelSize} for the pixel dims when
you only need width/height).
}
\examples{
fixture <- system.file("extdata", "fixtures", "image.pdf",
  package = "pdfium"
)
if (nzchar(fixture)) {
  doc <- pdf_doc_open(fixture)
  page <- pdf_page_load(doc, 1L)
  imgs <- Filter(function(o) o$type == "image", pdf_page_objects(page))
  if (length(imgs) > 0L) pdf_image_info(imgs[[1L]])
  pdf_page_close(page)
  pdf_doc_close(doc)
}
}
\seealso{
\code{\link[=pdf_image_bitmap]{pdf_image_bitmap()}} for the decoded pixels,
\code{\link[=pdf_image_rendered]{pdf_image_rendered()}} for the page-CTM-applied rendering,
\code{\link[=pdf_image_data]{pdf_image_data()}} for the raw stream bytes.
}
