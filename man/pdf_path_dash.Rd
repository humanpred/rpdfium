% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/paths.R
\name{pdf_path_dash}
\alias{pdf_path_dash}
\title{Dash pattern of a path page-object}
\usage{
pdf_path_dash(obj)
}
\arguments{
\item{obj}{A \code{pdfium_obj} of type \code{"path"} (from
\code{\link[=pdf_page_objects]{pdf_page_objects()}}).}
}
\value{
A named list with two elements:
\itemize{
\item \code{array} - numeric vector of dash lengths in PDF points;
length-zero for solid lines.
\item \code{phase} - numeric scalar, the dash-pattern phase in points
(typically \code{0}).
}
}
\description{
Returns the dash array (in PDF points) and dash phase (offset
into the pattern, in points) attached to \code{obj}'s stroke. A solid
(un-dashed) path returns an empty \code{array} and phase \code{0}.
}
\details{
A dash array of \code{c(3, 2)} for example means: draw 3 points, skip
2 points, repeat. The phase shifts where in the pattern the
first segment starts.
}
\examples{
fixture <- system.file("extdata", "fixtures", "shapes.pdf",
  package = "pdfium"
)
if (nzchar(fixture)) {
  doc <- pdf_open(fixture)
  p <- pdf_load_page(doc, 1)
  path_obj <- Filter(\(o) o$type == "path", pdf_page_objects(p))[[1]]
  pdf_path_dash(path_obj)
  pdf_close_page(p)
  pdf_close(doc)
}
}
\seealso{
\code{\link[=pdf_path_stroke]{pdf_path_stroke()}} for the stroke color and width.
}
