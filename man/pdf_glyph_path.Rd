% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/glyph_paths.R
\name{pdf_glyph_path}
\alias{pdf_glyph_path}
\title{Glyph outline for a single glyph in a text page-object's font}
\usage{
pdf_glyph_path(obj, glyph_code, font_size = NA_real_)
}
\arguments{
\item{obj}{A \code{pdfium_obj} of type \code{"text"}.}

\item{glyph_code}{Single non-negative integer; see the section
above.}

\item{font_size}{Numeric font size in PDF points. When \code{NA}
(default), uses the text object's own font size — the most
common choice when matching what is drawn on the page.}
}
\value{
A tibble with one row per glyph-path segment:
\itemize{
\item \code{segment_index} integer - 1-based.
\item \code{segment_type} character - \code{"moveto"}, \code{"lineto"},
\code{"bezierto"}, or \code{"unknown"}.
\item \code{x}, \code{y} numeric - point coordinates in PDF user-space
points (the glyph's local coordinate system, scaled to the
requested \code{font_size}).
\item \code{close_figure} logical - \code{TRUE} if this segment closes the
current sub-path.
Returns an empty tibble when PDFium reports no glyph outline.
}
}
\description{
Returns the path segments of the glyph rendered at \code{font_size}
in PDF user-space points. Useful for:
}
\details{
\itemize{
\item Reconstructing challenging character mappings — render the
glyph at the character's reported unicode code point and
compare to a reference rendering of that code point to see
whether the font actually draws what its ToUnicode CMap
claims.
\item Visualising the glyphs PDFium picked when extracting text.
\item Computing exact glyph silhouettes for layout / collision
detection beyond what bounding boxes give you.
}

Wraps \code{FPDFTextObj_GetFont} -> \code{FPDFFont_GetGlyphPath} ->
\code{FPDFGlyphPath_CountGlyphSegments} /
\code{FPDFGlyphPath_GetGlyphPathSegment}.
}
\section{Glyph code interpretation}{


\code{glyph_code} is the \emph{font's} glyph identifier, not the unicode
code point — though for many fonts they coincide:
\itemize{
\item \strong{TrueType fonts with \verb{/Identity-H} encoding} (most modern
embedded CID-keyed fonts): glyph code equals unicode code
point. Pass \code{chars$codepoint} from \code{\link[=pdf_text_chars]{pdf_text_chars()}}.
\item \strong{TrueType fonts with a \code{cmap} (e.g. WinAnsi or MacRoman
encoding)}: glyph code is the encoded character code in the
PDF stream, not the unicode value. The unicode <-> glyph map
is opaque through the public PDFium API.
\item \strong{Type 1 fonts}: glyph code is the encoding-specific
character code (1-byte for almost all PDF Type 1 fonts).
}

If the path comes back empty, the glyph code likely doesn't map
to a glyph in this font's encoding — try the character code
from the source content stream (visible in tools like \code{pdfinfo -text}) instead.
}

\examples{
\dontrun{
doc <- pdf_doc_open("weird-font.pdf")
page <- pdf_page_load(doc, 1)
text_obj <- Filter(\(o) o$type == "text", pdf_page_objects(page))[[1]]
# First visible character on the page:
chars <- pdf_text_chars(page)
first <- chars[!chars$is_generated, ][1, ]
pdf_glyph_path(text_obj, first$codepoint)
}
}
\seealso{
\code{\link[=pdf_glyph_width]{pdf_glyph_width()}}, \code{\link[=pdf_text_font_metrics]{pdf_text_font_metrics()}},
\code{\link[=pdf_text_chars]{pdf_text_chars()}} for the per-character readout that drives
most "investigate this glyph" workflows,
\code{\link[=pdf_text_obj_rendered_bitmap]{pdf_text_obj_rendered_bitmap()}} when you want the rendered
pixels instead of the outline.
}
