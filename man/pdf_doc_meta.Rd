% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/document.R
\name{pdf_doc_meta}
\alias{pdf_doc_meta}
\title{Read one entry from a PDF's Info dictionary}
\usage{
pdf_doc_meta(doc, tag)
}
\arguments{
\item{doc}{A \code{pdfium_doc} from \code{\link[=pdf_open]{pdf_open()}}.}

\item{tag}{Character scalar - the Info-dictionary key.}
}
\value{
Character scalar, UTF-8 encoded. \code{""} if the tag is not
present.
}
\description{
Wraps \code{FPDF_GetMetaText}. Returns the requested standard or
custom Info-dictionary tag value as a UTF-8 string, or \code{""}
when the tag is absent. Standard tags are \code{"Title"}, \code{"Author"},
\code{"Subject"}, \code{"Keywords"}, \code{"Creator"}, \code{"Producer"},
\code{"CreationDate"}, \code{"ModDate"}, \code{"Trapped"}. Custom tags from a
particular producer's Info dictionary are also accepted.
}
\examples{
fixture <- system.file("extdata", "fixtures", "shapes.pdf",
  package = "pdfium"
)
if (nzchar(fixture)) {
  doc <- pdf_open(fixture)
  pdf_doc_meta(doc, "Producer")
  pdf_close(doc)
}
}
\seealso{
\code{\link[=pdf_doc_info]{pdf_doc_info()}} for a single call that returns every
standard tag plus the page count and file version.
}
