% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/image_authoring.R
\name{pdf_image_new_from_bitmap}
\alias{pdf_image_new_from_bitmap}
\title{Embed a bitmap as an image page-object}
\usage{
pdf_image_new_from_bitmap(page, bitmap, bounds = NULL)
}
\arguments{
\item{page}{A \code{pdfium_page} or \code{pdfium_doc} opened with
\code{readwrite = TRUE}.}

\item{bitmap}{A \code{pdfium_image_buffer} from \code{\link[=pdf_bitmap_new]{pdf_bitmap_new()}}.}

\item{bounds}{Optional length-4 numeric vector \code{c(left, bottom, right, top)} in PDF user-space points. When supplied,
\code{FPDFImageObj_SetMatrix} positions the image at those bounds.
When \code{NULL}, the image sits at the unit square (\verb{[0,1] × [0,1]}) and the caller is responsible for setting a placement
matrix via \code{\link[=pdf_obj_set_matrix]{pdf_obj_set_matrix()}} afterwards.}
}
\value{
A \code{pdfium_obj} of type \code{"image"}.
}
\description{
Creates a blank image page-object on \code{page}, attaches the pixel
data in \code{bitmap} to it, optionally sets the placement matrix from
\code{bounds}, and inserts the result into the page. Companion to
\code{\link[=pdf_image_new]{pdf_image_new()}} (the JPEG path) for the raster path. The
returned handle is a \code{pdfium_obj} of type \code{"image"} like every
other page-level image.
}
\details{
The bitmap is copied into the page-object — callers are free to
\code{\link[=pdf_bitmap_close]{pdf_bitmap_close()}} the \code{pdfium_image_buffer} after this call
returns. (Without the copy, the bitmap's pixel buffer would
dangle once R garbage-collected the bitmap.)
}
\examples{
\dontrun{
doc <- pdf_doc_new()
page <- pdf_page_new(doc, width = 612, height = 792)
bm <- pdf_bitmap_new(32L, 32L, alpha = TRUE)
pdf_bitmap_fill_rect(bm, 0L, 0L, 32L, 32L, 0xFFFF0000)
pdf_image_new_from_bitmap(page, bm, bounds = c(72, 600, 200, 700))
pdf_bitmap_close(bm)
pdf_save(doc, tempfile(fileext = ".pdf"))
}
}
\seealso{
\code{\link[=pdf_image_new]{pdf_image_new()}} for the JPEG-bytes path,
\code{\link[=pdf_bitmap_new]{pdf_bitmap_new()}} for the bitmap constructor,
\code{\link[=pdf_image_set_bitmap]{pdf_image_set_bitmap()}} for the attach-only step.
}
