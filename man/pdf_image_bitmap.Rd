% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/images.R
\name{pdf_image_bitmap}
\alias{pdf_image_bitmap}
\title{Decoded image bitmap}
\usage{
pdf_image_bitmap(obj)
}
\arguments{
\item{obj}{A \code{pdfium_obj} of type \code{"image"}.}
}
\value{
A \code{pdfium_bitmap} (integer matrix with class
\code{c("pdfium_bitmap", "nativeRaster")}, \code{dim = c(height, width)})
carrying attributes \code{dpi = NA_real_} (the source image's DPI is
in \code{\link[=pdf_image_info]{pdf_image_info()}} but doesn't apply to this raw raster),
\code{source_page}, \code{source_path}, and \code{rotation_applied = 0L}. Use
\code{\link[=as.array.pdfium_bitmap]{as.array.pdfium_bitmap()}} / \code{\link[=as.raster.pdfium_bitmap]{as.raster.pdfium_bitmap()}} to
convert to other shapes.
}
\description{
Returns the embedded image's decoded pixel buffer as a
\code{\link[=pdf_render_page]{pdf_render_page()}}-compatible \code{pdfium_bitmap}. Wraps
\code{FPDFImageObj_GetBitmap}, which decodes the source stream but does
not apply the page's coordinate transformation - the bitmap is the
raw source raster, oriented in the image's own coordinate system.
}
\seealso{
\code{\link[=pdf_image_rendered]{pdf_image_rendered()}} for the CTM-applied rendering,
\code{\link[=pdf_image_data]{pdf_image_data()}} for the raw embedded stream bytes.
}
