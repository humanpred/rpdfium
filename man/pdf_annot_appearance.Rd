% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annot_probes.R
\name{pdf_annot_appearance}
\alias{pdf_annot_appearance}
\title{Appearance-stream string for an annotation}
\usage{
pdf_annot_appearance(annot, mode = c("normal", "rollover", "down"))
}
\arguments{
\item{annot}{A \code{pdfium_annot} handle (e.g. one element of
\code{pdf_annotations(page)}).}

\item{mode}{One of \code{"normal"} (default), \code{"rollover"}, or
\code{"down"}.}
}
\value{
Character scalar — the appearance-stream content, or
\code{""} when no appearance is set for the requested mode.
}
\description{
Returns the contents of an annotation's \verb{/AP} appearance stream
for the requested appearance mode. PDF annotations can carry up
to three appearance streams: \code{"normal"} (default, drawn at
rest), \code{"rollover"} (drawn while the cursor hovers), and
\code{"down"} (drawn while the annotation is being activated).
Wraps \code{FPDFAnnot_GetAP}.
}
\details{
Useful for analysing or rebuilding custom annotations whose
appearance can't be reconstructed from the structural metadata
\code{\link[=pdf_annotations]{pdf_annotations()}} surfaces (color / border / quad points).
}
\seealso{
\code{\link[=pdf_annotations]{pdf_annotations()}}, \code{\link[=pdf_annot_dict_value]{pdf_annot_dict_value()}}.
}
