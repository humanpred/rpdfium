% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mutation.R
\name{pdf_pages_reorder}
\alias{pdf_pages_reorder}
\title{Reorder pages}
\usage{
pdf_pages_reorder(doc, new_order = NULL, move_pages = NULL, dest = NULL)
}
\arguments{
\item{doc}{A read-write \code{pdfium_doc}.}

\item{new_order}{Integer vector. A full permutation of
\code{1:pdf_page_count(doc)}.}

\item{move_pages}{Integer vector of 1-based source page
indices to move (ignored when \code{new_order} is supplied).}

\item{dest}{One-based destination index for the moved pages
(ignored when \code{new_order} is supplied).}
}
\value{
Invisibly returns \code{doc}.
}
\description{
Wraps \code{FPDF_MovePages}. Either passes a full permutation of
\code{seq_len(pdf_page_count(doc))} as \code{new_order} (the document is
reordered in place to that permutation), or moves a contiguous
set of pages to a new position via \code{move_pages} + \code{dest}.
}
