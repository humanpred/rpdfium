% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/text.R
\name{pdf_text_content}
\alias{pdf_text_content}
\title{Text content of a text page-object}
\usage{
pdf_text_content(obj)
}
\arguments{
\item{obj}{A \code{pdfium_obj} of type \code{"text"} (from
\code{\link[=pdf_page_objects]{pdf_page_objects()}}).}
}
\value{
A character scalar (UTF-8 encoded). An empty text object
returns \code{""}.
}
\description{
Returns the Unicode text of \code{obj} as a single character string.
PDFium produces UTF-16LE internally; the wrapper converts to
UTF-8 with the encoding flag set so R prints non-ASCII glyphs
correctly.
}
\details{
Loading text from a PDF requires the per-page text-extraction
context (\code{FPDFText_LoadPage} / \code{FPDFText_ClosePage}). The wrapper
opens and closes that context internally on every call. When you
need many text objects from one page, the upcoming
\code{pdf_text_runs()} (Phase 3 slice 2) will share a single text-page
across the entire page to avoid the per-call overhead.
}
\examples{
fixture <- system.file("extdata", "fixtures", "shapes.pdf",
  package = "pdfium"
)
if (nzchar(fixture)) {
  doc <- pdf_doc_open(fixture)
  p <- pdf_page_load(doc, 1)
  text_obj <- Filter(\(o) o$type == "text", pdf_page_objects(p))[[1]]
  pdf_text_content(text_obj)
  pdf_page_close(p)
  pdf_doc_close(doc)
}
}
\seealso{
\code{\link[=pdf_text_font_size]{pdf_text_font_size()}}, \code{\link[=pdf_page_objects]{pdf_page_objects()}}
}
