% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/text_extras.R
\name{pdf_text_colors}
\alias{pdf_text_colors}
\title{Per-character fill and stroke colors and text-index mapping}
\usage{
pdf_text_colors(page, page_num = 1L)
}
\arguments{
\item{page}{A \code{pdfium_page} from \code{\link[=pdf_page_load]{pdf_page_load()}}, or a
\code{pdfium_doc} (the page given by \code{page_num} will be loaded and
closed internally).}

\item{page_num}{One-based page index. Only used when \code{page} is a
\code{pdfium_doc}. Ignored otherwise.}
}
\value{
A tibble with one row per character and columns
\code{char_index} (1-based), \code{text_index} (0-based index in the
page's extracted text; \code{NA} for generated/hyphen/formatting
chars), \code{fill_red}, \code{fill_green}, \code{fill_blue}, \code{fill_alpha},
\code{stroke_red}, \code{stroke_green}, \code{stroke_blue}, \code{stroke_alpha}
(0-255 integers, \code{NA} when PDFium reports failure).
}
\description{
Returns one row per character on the page with the fill / stroke
RGBA colour PDFium reports for that glyph and the text-position
the character occupies in the page's extracted text. Suitable for
joining onto \code{\link[=pdf_text_chars]{pdf_text_chars()}} by \code{char_index}.
}
\details{
Use cases:
\itemize{
\item Detect invisible / clip-mode text (alpha = 0 in fill \emph{and}
stroke) for text-extraction quality checks.
\item Distinguish styled-text passages (e.g. highlights with a
non-default fill alpha).
\item Translate between the character-index space PDFium uses
internally and the extracted-text index space that
\code{\link[=pdf_text_search]{pdf_text_search()}}'s \code{start_char} aligns with — characters
with \code{text_index = NA} are generated / hyphen / formatting
chars that don't appear in the rendered text string.
}

Wraps \code{FPDFText_GetFillColor}, \code{FPDFText_GetStrokeColor}, and
\code{FPDFText_GetTextIndexFromCharIndex}.
}
\seealso{
\code{\link[=pdf_text_chars]{pdf_text_chars()}} (per-char geometry / codepoint),
\code{\link[=pdf_text_render_mode]{pdf_text_render_mode()}} (per-text-object render mode).
}
