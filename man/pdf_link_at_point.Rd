% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/page_nav.R
\name{pdf_link_at_point}
\alias{pdf_link_at_point}
\title{Hit-test for the link annotation under a point}
\usage{
pdf_link_at_point(page, x, y, page_num = 1L)
}
\arguments{
\item{page}{A \code{pdfium_page} from \code{\link[=pdf_page_load]{pdf_page_load()}}, or a
\code{pdfium_doc} (the page given by \code{page_num} will be loaded
and closed internally).}

\item{x, y}{Point coordinates in PDF user-space points.}

\item{page_num}{One-based page index. Only used when \code{page} is a
\code{pdfium_doc}. Ignored otherwise.}
}
\value{
A tibble with at most one row. Columns:
\itemize{
\item \code{z_order} — integer, the link's Z-order on the page
(higher = on top).
\item \code{left}, \code{bottom}, \code{right}, \code{top} — link's rectangle in PDF
points.
\item \code{action_type} — character: \code{"goto"}, \code{"remote_goto"},
\code{"uri"}, \code{"launch"}, \code{"embedded_goto"}, or \code{"unsupported"}.
\item \code{uri} — the link target URI when \code{action_type == "uri"},
\code{NA} otherwise.
\item \code{filepath} — the external file path when \code{action_type} is
\code{"remote_goto"} / \code{"launch"} / \code{"embedded_goto"}, \code{NA}
otherwise.
\item \code{dest_page} — the resolved 1-based target page for any GoTo
action (\code{NA} if not resolvable).
}

Empty tibble (0 rows) when no link sits under the point.
}
\description{
Finds the link annotation at PDF user-space coordinates \verb{(x, y)}
on a page. Useful for translating a click on a rendered PDF back
to its semantic target. Wraps \code{FPDFLink_GetLinkAtPoint} plus the
\code{FPDFLink_GetLinkZOrderAtPoint} / \code{FPDFLink_GetAction} /
\verb{FPDFAction_*} family.
}
\details{
Coordinates are in PDF user-space points (origin at the page's
bottom-left; page width and height in points come from
\code{\link[=pdf_page_size]{pdf_page_size()}}).
}
\seealso{
\code{\link[=pdf_page_links]{pdf_page_links()}} for the full enumeration.
}
