% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/name_lookups.R
\name{pdf_named_dest_by_name}
\alias{pdf_named_dest_by_name}
\title{Resolve a named destination by name}
\usage{
pdf_named_dest_by_name(doc, name, password = NULL)
}
\arguments{
\item{doc}{A \code{pdfium_doc} from \code{\link[=pdf_open]{pdf_open()}}, or a character path.}

\item{name}{Single non-empty character string.}

\item{password}{Optional password for encrypted PDFs when \code{doc}
is a path. Ignored when \code{doc} is already an open \code{pdfium_doc}.}
}
\value{
A list with \code{found} (logical), \code{page} (integer, 1-based,
\code{NA} when not resolvable), and \code{dest_view} / \code{dest_x} /
\code{dest_y} / \code{dest_zoom} (same shape as the corresponding
columns on \code{\link[=pdf_named_dests]{pdf_named_dests()}}). \code{found = FALSE} and all
other fields \code{NA} when the name is not in the destination
table.
}
\description{
Looks up a \verb{/Dest} by its name string and returns the same kind
of row \code{\link[=pdf_named_dests]{pdf_named_dests()}} surfaces — page, view, x, y, zoom.
Useful for following cross-document references such as
\code{RemoteGoTo} actions that carry a destination name rather than
a page index.
}
\details{
Wraps \code{FPDF_GetNamedDestByName} plus \code{FPDFDest_GetDestPageIndex}
/ \code{FPDFDest_GetView} / \code{FPDFDest_GetLocationInPage}.
}
\seealso{
\code{\link[=pdf_named_dests]{pdf_named_dests()}}.
}
