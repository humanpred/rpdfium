% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tier3_extras.R
\name{pdf_text_char_obj_index}
\alias{pdf_text_char_obj_index}
\title{Reverse-map a character index to its page-object index}
\usage{
pdf_text_char_obj_index(page, char_index, page_num = 1L)
}
\arguments{
\item{page}{A \code{pdfium_page} from \code{\link[=pdf_page_load]{pdf_page_load()}}, or a
\code{pdfium_doc}.}

\item{char_index}{One-based character index (matches
\code{pdf_text_chars()$char_index}).}

\item{page_num}{One-based page index. Only used when \code{page} is
a \code{pdfium_doc}. Ignored otherwise.}
}
\value{
Integer scalar — the 1-based page-object index, or \code{NA}
when the character has no associated page object (e.g.
PDFium-synthesised whitespace).
}
\description{
Given a 1-based \code{char_index} on the page's text page (matching
the \code{char_index} column of \code{\link[=pdf_text_chars]{pdf_text_chars()}}), return the
1-based page-object index of the text run that contains it.
Wraps \code{FPDFText_GetTextObject} plus a lookup into the page's
object table.
}
\details{
Useful for jumping from a per-character readout back to the
parent text page object's style / position metadata in
\code{\link[=pdf_text_runs]{pdf_text_runs()}} (which uses the same \code{obj_index}).
}
\seealso{
\code{\link[=pdf_text_chars]{pdf_text_chars()}}, \code{\link[=pdf_text_runs]{pdf_text_runs()}}.
}
