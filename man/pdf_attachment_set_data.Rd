% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/attachment_authoring.R
\name{pdf_attachment_set_data}
\alias{pdf_attachment_set_data}
\title{Set the raw bytes of an embedded file attachment}
\usage{
pdf_attachment_set_data(att, data)
}
\arguments{
\item{att}{A \code{pdfium_attachment} from \code{\link[=pdf_attachments]{pdf_attachments()}} or
\code{\link[=pdf_attachment_new]{pdf_attachment_new()}}. Parent doc must be readwrite.}

\item{data}{A raw vector of file bytes. To attach a UTF-8 text
payload, pass \code{charToRaw(enc2utf8(text))}.}
}
\value{
Invisibly returns the parent \code{pdfium_doc}.
}
\description{
Replaces the attachment's embedded file data with the given raw
bytes. Wraps \code{FPDFAttachment_SetFile}. The attachment's
\code{CreationDate} and checksum dictionary entries are automatically
updated; \strong{all other entries} (including the MIME \code{Subtype} and
the \code{Desc} you may have set with
\code{\link[=pdf_attachment_set_dict_value]{pdf_attachment_set_dict_value()}}) are cleared by PDFium during
the write — set those entries \emph{after} this call.
}
\details{
Use this immediately after \code{\link[=pdf_attachment_new]{pdf_attachment_new()}} to populate a
fresh attachment, or to update the file contents of an existing
one.
}
\seealso{
\code{\link[=pdf_attachment_data]{pdf_attachment_data()}} for the read side.
}
