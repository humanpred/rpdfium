% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annotations.R
\name{as_tibble.pdfium_annot_list}
\alias{as_tibble.pdfium_annot_list}
\title{Tibble view of a \code{pdfium_annot_list}}
\usage{
\method{as_tibble}{pdfium_annot_list}(x, ...)
}
\arguments{
\item{x}{A \code{pdfium_annot_list} from \code{\link[=pdf_annotations]{pdf_annotations()}}.}

\item{...}{Unused (S3 generic compatibility).}
}
\value{
A tibble with one row per annotation. Columns mirror
the previous \code{pdf_annotations()} tibble plus \code{handle} and
\code{source}.
}
\description{
Walks the list of annotation handles and reads every documented
attribute into a wide tibble. The tibble carries two extra
list-columns relative to a simple data extraction:
}
\details{
\itemize{
\item \code{handle} — the original \code{pdfium_annot} handle for that row,
so the round-trip back via \code{\link[=as_pdfium_annot_list]{as_pdfium_annot_list()}} preserves
R-object identity.
\item \code{source} — the parent \code{pdfium_page} for every row.
}
}
