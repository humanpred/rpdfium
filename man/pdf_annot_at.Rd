% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annot_class.R
\name{pdf_annot_at}
\alias{pdf_annot_at}
\title{Construct a \code{pdfium_annot} handle for one annotation}
\usage{
pdf_annot_at(page, annotation_index, page_num = 1L)
}
\arguments{
\item{page}{A \code{pdfium_page} or \code{pdfium_doc}.}

\item{annotation_index}{One-based annotation index on the page.}

\item{page_num}{One-based page index (only used when \code{page} is a
\code{pdfium_doc}).}
}
\value{
A \code{pdfium_annot} handle.
}
\description{
Looks up the annotation at \code{annotation_index} on \code{page} and
returns a handle. Wraps \code{FPDFPage_GetAnnot}.
}
\details{
Most callers don't need this directly — \code{\link[=pdf_annotations]{pdf_annotations()}}
returns the full list of handles. \code{pdf_annot_at()} is the
targeted lookup, useful when you have an index from a tibble row.
}
