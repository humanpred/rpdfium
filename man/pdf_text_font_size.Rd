% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/text.R
\name{pdf_text_font_size}
\alias{pdf_text_font_size}
\title{Font size of a text page-object}
\usage{
pdf_text_font_size(obj)
}
\arguments{
\item{obj}{A \code{pdfium_obj} of type \code{"text"} (from
\code{\link[=pdf_page_objects]{pdf_page_objects()}}).}
}
\value{
Numeric scalar in PDF points, or \code{NA_real_} if PDFium
reports no font size (rare; usually only for malformed PDFs).
}
\description{
Returns the font size, in PDF points, of \code{obj}. PDFium's API
reports the post-CTM rendering size - i.e. the value a viewer
would draw with after applying the page's transformation matrix.
}
\examples{
fixture <- system.file("extdata", "fixtures", "shapes.pdf",
                       package = "pdfium")
if (nzchar(fixture)) {
  doc <- pdf_open(fixture)
  p <- pdf_load_page(doc, 1)
  text_obj <- Filter(\(o) o$type == "text", pdf_page_objects(p))[[1]]
  pdf_text_font_size(text_obj)
  pdf_close_page(p)
  pdf_close(doc)
}
}
\seealso{
\code{\link[=pdf_page_objects]{pdf_page_objects()}}
}
