% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/page_thumbs.R
\name{pdf_page_thumbnail}
\alias{pdf_page_thumbnail}
\title{Page embedded thumbnail}
\usage{
pdf_page_thumbnail(page, page_num = 1L, decoded = TRUE)
}
\arguments{
\item{page}{A \code{pdfium_page} from \code{\link[=pdf_page_load]{pdf_page_load()}}, or a
\code{pdfium_doc}.}

\item{page_num}{One-based page index. Only used when \code{page} is a
\code{pdfium_doc}. Ignored otherwise.}

\item{decoded}{If \code{TRUE} (default) returns the decoded bitmap
bytes after PDFium has applied any stream filter (e.g.
\verb{/FlateDecode}). If \code{FALSE}, returns the raw filtered bytes —
useful when a caller wants to save the thumbnail back to disk
in its original encoded form, or pipe it through a different
decoder.}
}
\value{
A \code{raw} vector. Length zero when the page has no \verb{/Thumb}.
}
\description{
Returns the bytes of a page's embedded \verb{/Thumb} image stream, if
the PDF carries one. PDF authoring tools sometimes embed a
low-resolution preview alongside each page; readers can display
that thumbnail without rendering the full page. Wraps
\code{FPDFPage_GetRawThumbnailData} and \code{FPDFPage_GetDecodedThumbnailData}.
}
\details{
Most PDFs produced by Cairo, LaTeX, or web tools do not embed
thumbnails — this function returns \code{raw(0)} in that common case.
}
\seealso{
\code{\link[=pdf_render_page]{pdf_render_page()}} to rasterize the full page instead.
}
