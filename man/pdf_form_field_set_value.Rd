% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/form_field_setters.R
\name{pdf_form_field_set_value}
\alias{pdf_form_field_set_value}
\title{Set the value of a form field}
\usage{
pdf_form_field_set_value(field, value)
}
\arguments{
\item{field}{A \code{pdfium_form_field} from \code{\link[=pdf_form_fields]{pdf_form_fields()}}.
Parent doc must be readwrite.}

\item{value}{Character scalar OR logical scalar (for checkable
types). See type-specific rules above.}
}
\value{
Invisibly returns the parent \code{pdfium_doc}.
}
\description{
Polymorphic setter: the semantics depend on the field's type.
}
\details{
\itemize{
\item \strong{Text} (\code{"textfield"}, \code{"xfa_textfield"}): \code{value} must be a
character scalar. Sets \verb{/V} directly.
\item \strong{Checkbox / radio} (\code{"checkbox"}, \code{"radiobutton"},
\code{"xfa_checkbox"}): \code{value} may be either a logical scalar or a
character scalar. \code{TRUE} writes the field's on-state name
(inferred from the current \verb{/V} or \verb{/AS}, falling back to
\code{"Yes"}); \code{FALSE} writes \code{"Off"}. A character value is written
literally — useful when you already know the export-value
string and want to bypass inference.
\item \strong{Combobox / listbox} (\code{"combobox"}, \code{"listbox"},
\code{"xfa_combobox"}, \code{"xfa_listbox"}): \code{value} must be a character
scalar matching one of the field's options
(\code{\link[=pdf_form_field_options]{pdf_form_field_options()}}).
}

Any other field type (button / signature / unknown) errors —
those don't have a settable value.

Wraps \code{FPDFAnnot_SetStringValue(annot, "V", ...)} followed by a
rect re-touch (\code{FPDFAnnot_SetRect} to the current rect) that
flips the AP-dirty flag, so the next \code{\link[=pdf_render_page]{pdf_render_page()}} or
\code{\link[=pdf_save]{pdf_save()}} rebuilds the widget's appearance stream from the
new value.
}
\seealso{
\code{\link[=pdf_form_field_value]{pdf_form_field_value()}}, \code{\link[=pdf_form_field_clear]{pdf_form_field_clear()}},
\code{\link[=pdf_form_reset]{pdf_form_reset()}}.
}
