% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/page_extras.R
\name{pdf_text_chars}
\alias{pdf_text_chars}
\title{Per-character text extraction}
\usage{
pdf_text_chars(page, page_num = 1L)
}
\arguments{
\item{page}{A \code{pdfium_page} from \code{\link[=pdf_load_page]{pdf_load_page()}}, or a
\code{pdfium_doc}.}

\item{page_num}{One-based page index. Only used when \code{page} is
a \code{pdfium_doc}. Ignored otherwise.}
}
\value{
A tibble with columns:
\itemize{
\item \code{char_index} integer - 1-based position in the page's
character stream.
\item \code{codepoint} integer - Unicode code point.
\item \code{char} character - UTF-8 character; empty for surrogate
halves or PDFium's NUL sentinel.
\item \code{bounds_left}, \code{bounds_bottom}, \code{bounds_right},
\code{bounds_top} - glyph bounding box in PDF user space.
\item \code{font_size} numeric - effective glyph height in user-space
points (the run's font size times the text matrix scale).
\item \code{is_generated} logical - \code{TRUE} for whitespace PDFium
synthesised between positioned glyphs (the source PDF
does not carry a character there; PDFium infers one for
text-extraction consumers).
\item \code{is_hyphen} logical - \code{TRUE} for end-of-line soft hyphens.
\item \code{origin_x}, \code{origin_y} - the character's glyph origin point
in PDF user space (\code{FPDFText_GetCharOrigin}). Distinct from
the bounding-box corners; for many fonts the origin is at
the baseline left of the glyph.
\item \code{loose_left}, \code{loose_bottom}, \code{loose_right}, \code{loose_top} -
the "loose" bounding box covering the entire glyph cell
(font ascent / descent included), not just the glyph
outline. Use these when you need consistent line heights;
use \verb{bounds_*} for the tight glyph extent.
\item \code{unicode_map_error} logical - \code{TRUE} when PDFium detected
that the character's ToUnicode CMap is malformed for this
glyph (the codepoint reported may be the PDF's `
fallback rather than the intended character).
\item \code{text_index} integer - 0-based position in the \emph{extractable}
text string (i.e. the linear \code{pdf_text()} output) for this
character, or \code{NA} for synthesised whitespace and other
characters that don't appear in the extracted text.
}

Returns a 0-row tibble of the same schema when the page has no
text.
}
\description{
Returns one tibble row per character on the page, with the
character's Unicode codepoint and UTF-8 form, glyph bounding
box, effective font size, and two PDF flags indicating
"generated" characters (whitespace PDFium inferred between
positioned glyphs) and end-of-line "soft" hyphens. Wraps
\code{FPDFText_LoadPage} plus \code{FPDFText_CountChars} /
\verb{_GetUnicode} / \verb{_GetCharBox} / \verb{_GetFontSize} /
\verb{_IsGenerated} / \verb{_IsHyphen}.
}
\details{
This is the per-character analog of \code{\link[=pdf_text_runs]{pdf_text_runs()}}
(per-text-object) and \code{\link[=pdf_text]{pdf_text()}} (per-page). The three
coexist: use \code{pdf_text()} when you just want the strings,
\code{pdf_text_runs()} for object-level positions, and
\code{pdf_text_chars()} when you need glyph-level geometry (e.g.
word segmentation, character-by-character layout analysis).
}
\seealso{
\code{\link[=pdf_text_runs]{pdf_text_runs()}}, \code{\link[=pdf_text]{pdf_text()}}.
}
