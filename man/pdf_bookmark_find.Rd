% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/name_lookups.R
\name{pdf_bookmark_find}
\alias{pdf_bookmark_find}
\title{Find a bookmark by its title}
\usage{
pdf_bookmark_find(doc, title, password = NULL)
}
\arguments{
\item{doc}{A \code{pdfium_doc} from \code{\link[=pdf_open]{pdf_open()}}, or a character path.}

\item{title}{Single non-empty character string.}

\item{password}{Optional password for encrypted PDFs when \code{doc}
is a path. Ignored when \code{doc} is already an open \code{pdfium_doc}.}
}
\value{
Integer scalar — the 1-based bookmark_index, or \code{NA}.
}
\description{
Returns the 1-based \code{bookmark_index} of the first outline entry
matching \code{title}, suitable for indexing back into
\code{\link[=pdf_bookmarks]{pdf_bookmarks()}}'s tibble. \code{NA} when no bookmark matches.
Wraps \code{FPDFBookmark_Find} and walks the outline pre-order to map
the PDFium handle back to the row index.
}
\details{
PDFium's matching is case-sensitive and matches the full title
string.
}
\seealso{
\code{\link[=pdf_bookmarks]{pdf_bookmarks()}}.
}
