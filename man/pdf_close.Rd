% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/document.R
\name{pdf_close}
\alias{pdf_close}
\title{Close a PDF document}
\usage{
pdf_close(doc)
}
\arguments{
\item{doc}{A \code{pdfium_doc} produced by \code{\link[=pdf_open]{pdf_open()}}.}
}
\value{
Invisibly returns \code{doc} with its underlying pointer marked closed.
}
\description{
Releases the underlying PDFium handle. Idempotent — calling \code{pdf_close()} on
an already-closed document is a no-op. The finalizer registered at
\code{\link[=pdf_open]{pdf_open()}} also calls this when the R object is garbage-collected, but
explicit close is recommended when handling many large documents or when a
subsequent operation needs to delete the source file (relevant on Windows).
}
