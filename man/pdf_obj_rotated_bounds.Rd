% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/obj_extras.R
\name{pdf_obj_rotated_bounds}
\alias{pdf_obj_rotated_bounds}
\title{Rotated bounding quadpoints of a page object}
\usage{
pdf_obj_rotated_bounds(obj)
}
\arguments{
\item{obj}{A \code{pdfium_obj} from \code{\link[=pdf_page_objects]{pdf_page_objects()}}.}
}
\value{
A length-8 named numeric vector
\code{c(x1, y1, x2, y2, x3, y3, x4, y4)} in PDF user-space points,
or all-\code{NA} when PDFium reports no bounds for this object.
}
\description{
For objects that have been rotated by a transformation matrix
(e.g. text drawn at an angle, or a placed image with a rotated
Form XObject CTM), the axis-aligned bounding box from
\code{\link[=pdf_obj_bounds]{pdf_obj_bounds()}} is loose. \code{pdf_obj_rotated_bounds()} returns
the tighter rotated rectangle as four corner points. Wraps
\code{FPDFPageObj_GetRotatedBounds}.
}
\details{
The four corners are returned in the order PDFium reports them:
\verb{(x1, y1)} is lower-left, \verb{(x2, y2)} lower-right, \verb{(x3, y3)}
upper-right, \verb{(x4, y4)} upper-left, where "lower" / "upper" are
relative to the rotated rectangle's own local axes (not the page).
}
\seealso{
\code{\link[=pdf_obj_bounds]{pdf_obj_bounds()}} for the cheaper axis-aligned box.
}
