% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annot_probes.R
\name{pdf_annot_dict_value}
\alias{pdf_annot_dict_value}
\title{Read an annotation-dict entry by key}
\usage{
pdf_annot_dict_value(page, annotation_index, key, page_num = 1L)
}
\arguments{
\item{page}{A \code{pdfium_page} from \code{\link[=pdf_page_load]{pdf_page_load()}}, or a
\code{pdfium_doc}.}

\item{annotation_index}{One-based index into the page's
annotations.}

\item{key}{The annotation-dict key as a single non-empty
character string (ASCII PDF name, e.g. \code{"M"}, \code{"NM"}, \code{"CA"}).}

\item{page_num}{One-based page index. Only used when \code{page} is a
\code{pdfium_doc}. Ignored otherwise.}
}
\value{
A list with four fields:
\itemize{
\item \code{has_key} (logical) — \code{TRUE} when the annotation dict
contains \code{key}.
\item \code{value_type} (integer) — PDFium's \verb{FPDF_OBJECT_*} enum
value (\code{0}=unknown, \code{1}=boolean, \code{2}=number, \code{3}=string,
\code{4}=name, ...); \code{NA} when the key is absent.
\item \code{value_string} (character) — populated when the value is a
PDF string or name; \code{NA_character_} otherwise.
\item \code{value_number} (numeric) — populated when the value is a
PDF number; \code{NA_real_} otherwise.
}
}
\description{
Parallel to \code{\link[=pdf_attachment_dict_value]{pdf_attachment_dict_value()}} but for annotations.
Returns the typed value of a key in the annotation's
dictionary — useful for ad-hoc access to keys \code{\link[=pdf_annotations]{pdf_annotations()}}
doesn't surface (e.g. \verb{/M} modification date, \verb{/NM} unique name,
\verb{/CA} overall opacity, \verb{/RC} rich-text contents).
}
\details{
Wraps \code{FPDFAnnot_HasKey}, \code{FPDFAnnot_GetValueType},
\code{FPDFAnnot_GetStringValue}, and \code{FPDFAnnot_GetNumberValue}. Only
string-, name-, and number-typed values come back; other
value types (dict / array / stream / reference) report
\code{value_type} accordingly but leave the typed accessors as \code{NA}.
}
\seealso{
\code{\link[=pdf_annotations]{pdf_annotations()}} for the structured per-annotation
readout, \code{\link[=pdf_annot_appearance]{pdf_annot_appearance()}} for the \verb{/AP} appearance
stream.
}
