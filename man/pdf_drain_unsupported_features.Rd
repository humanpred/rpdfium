% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/doc_extra.R
\name{pdf_drain_unsupported_features}
\alias{pdf_drain_unsupported_features}
\title{Read and clear the PDFium unsupported-feature event buffer}
\usage{
pdf_drain_unsupported_features()
}
\value{
Character vector of feature names. Each entry is a human-
readable string drawn from the \verb{FPDF_UNSP_*} enum, e.g.
\code{"XFA form"}, \code{"3D annotation"}, \code{"document security"},
\code{"signature annotation"}. Possibly contains duplicates if a
feature was flagged multiple times.
}
\description{
Returns the human-readable feature names PDFium has flagged as
unsupported since the previous call (or since the handler was
installed via \code{\link[=pdf_install_unsupported_handler]{pdf_install_unsupported_handler()}}). The buffer is
cleared on read.
}
\details{
Returns \code{character(0)} when nothing has been flagged or when the
handler is not installed (no events are buffered until
installation).
}
\seealso{
\code{\link[=pdf_install_unsupported_handler]{pdf_install_unsupported_handler()}} to enable the
handler.
}
