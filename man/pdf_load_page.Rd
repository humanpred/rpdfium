% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/page.R
\name{pdf_load_page}
\alias{pdf_load_page}
\title{Load a single page from an open PDF document}
\usage{
pdf_load_page(doc, page_num = 1L)
}
\arguments{
\item{doc}{A \code{pdfium_doc} from \code{\link[=pdf_open]{pdf_open()}}.}

\item{page_num}{One-based page index. Must satisfy
\verb{1 <= page_num <= pdf_page_count(doc)}.}
}
\value{
A \code{pdfium_page} object.
}
\description{
Returns a \code{pdfium_page} handle bound to its parent \code{doc}. The page is
garbage-collected with a finalizer that calls \code{FPDF_ClosePage}; call
\code{\link[=pdf_close_page]{pdf_close_page()}} explicitly when you need deterministic release.
The page keeps the parent document alive for as long as the page
is reachable, so it is safe to drop your reference to \code{doc} while
still holding a page.
}
\examples{
fixture <- system.file("extdata", "fixtures", "minimal.pdf",
                       package = "pdfium")
if (nzchar(fixture)) {
  doc <- pdf_open(fixture)
  page <- pdf_load_page(doc, 1)
  pdf_close_page(page)
  pdf_close(doc)
}
}
