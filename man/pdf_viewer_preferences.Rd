% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/doc_extra.R
\name{pdf_viewer_preferences}
\alias{pdf_viewer_preferences}
\title{Read the document's viewer preferences}
\usage{
pdf_viewer_preferences(doc, password = NULL)
}
\arguments{
\item{doc}{A \code{pdfium_doc} from \code{\link[=pdf_open]{pdf_open()}}, or a character path.}

\item{password}{Optional password for encrypted PDFs when \code{doc}
is a path. Ignored when \code{doc} is already an open \code{pdfium_doc}.}
}
\value{
A named list with:
\itemize{
\item \code{print_scaling} (logical) — TRUE if the author wants the
viewer's print dialog to use its default scaling.
\item \code{num_copies} (integer) — suggested copies; 1 if not set.
\item \code{duplex} (character) — one of \code{"none"}, \code{"simplex"},
\code{"duplex_flip_short_edge"}, \code{"duplex_flip_long_edge"}.
\item \code{print_page_ranges} (integer) — 1-based page numbers the
author suggests printing; empty when unspecified.
}
}
\description{
Returns the print-related preferences encoded in the PDF's
ViewerPreferences dictionary: whether the viewer should honor the
author's print scaling, the suggested number of copies, the
paper-handling (duplex) option, and the print-page-range
specification. Wraps the \verb{FPDF_VIEWERREF_*} family.
}
\details{
Most PDFs don't set these; the returned defaults are PDFium's
"no preference" sentinels: \code{print_scaling = TRUE},
\code{num_copies = 1}, \code{duplex = "none"}, \code{print_page_ranges} empty.
}
