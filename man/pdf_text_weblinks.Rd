% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/page_thumbs.R
\name{pdf_text_weblinks}
\alias{pdf_text_weblinks}
\title{Auto-detected web links in a page's text}
\usage{
pdf_text_weblinks(page, page_num = 1L)
}
\arguments{
\item{page}{A \code{pdfium_page} from \code{\link[=pdf_page_load]{pdf_page_load()}}, or a
\code{pdfium_doc}.}

\item{page_num}{One-based page index. Only used when \code{page} is a
\code{pdfium_doc}. Ignored otherwise.}
}
\value{
A tibble with one row per detected URL and columns:
\itemize{
\item \code{url} (character) — the matched URL string. UTF-8.
\item \code{start_char} (integer) — 0-based character offset of the URL
on the page's text page.
\item \code{char_count} (integer) — number of characters in the matched
span.
\item \code{left}, \code{bottom}, \code{right}, \code{top} (numeric) — axis-aligned
union of the URL's per-line rectangles in PDF user-space
points. \code{NA} when PDFium reports no bounds.
}

Returns a 0-row tibble of the same schema when no URLs are
detected.
}
\description{
Returns one row per URL that PDFium's web-link detector finds in
the page's extracted text. Detected patterns include \verb{http://...},
\verb{https://...}, \code{www.example.com}, and \code{mailto:user@host}. Wraps
\code{FPDFLink_LoadWebLinks} plus \code{FPDFLink_GetURL},
\code{FPDFLink_GetTextRange}, \code{FPDFLink_CountRects}, and
\code{FPDFLink_GetRect}.
}
\details{
This is distinct from \code{\link[=pdf_page_links]{pdf_page_links()}}, which enumerates the
clickable link \emph{annotations} declared by the PDF author. Use
\code{pdf_text_weblinks()} when the URL appears as plain text on the
page (no link annotation), and \code{pdf_page_links()} when you want
the explicit clickable regions.

Multi-line URLs produce one row whose bounding box is the
axis-aligned union of every contributing line's rectangle. If you
need a rectangle per line, pair \code{start_char} and \code{char_count} with
\code{\link[=pdf_text_chars]{pdf_text_chars()}} over \code{start_char:(start_char + char_count - 1L)}.
}
\seealso{
\code{\link[=pdf_page_links]{pdf_page_links()}} for link annotations,
\code{\link[=pdf_text_search]{pdf_text_search()}} for arbitrary string search.
}
