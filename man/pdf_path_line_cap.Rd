% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/obj_extras.R
\name{pdf_path_line_cap}
\alias{pdf_path_line_cap}
\title{Stroke line-cap style of a path page-object}
\usage{
pdf_path_line_cap(obj)
}
\arguments{
\item{obj}{A \code{pdfium_obj} of type \code{"path"} from
\code{\link[=pdf_page_objects]{pdf_page_objects()}}.}
}
\value{
Character scalar; one of \code{"butt"} (square cap aligned with
the stroke endpoint, the PDF default), \code{"round"} (semicircular
extension past the endpoint), or \code{"projecting_square"} (square
cap extending one half-line-width past the endpoint).
}
\description{
Returns the PDF line-cap style applied to a path's stroke. Maps to
the \code{LC} operand in the page content stream and corresponds to
PDFium's \code{FPDFPageObj_GetLineCap}.
}
\examples{
fixture <- system.file("extdata", "fixtures", "shapes.pdf",
                       package = "pdfium")
if (nzchar(fixture)) {
  doc <- pdf_open(fixture); p <- pdf_load_page(doc, 1)
  path_obj <- Filter(\(o) o$type == "path", pdf_page_objects(p))[[1]]
  pdf_path_line_cap(path_obj)
  pdf_close_page(p); pdf_close(doc)
}
}
\seealso{
\code{\link[=pdf_path_line_join]{pdf_path_line_join()}}, \code{\link[=pdf_path_stroke]{pdf_path_stroke()}}.
}
