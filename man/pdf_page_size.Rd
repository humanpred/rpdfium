% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/page.R
\name{pdf_page_size}
\alias{pdf_page_size}
\title{Page dimensions in PDF points}
\usage{
pdf_page_size(x, page = 1L)
}
\arguments{
\item{x}{A \code{pdfium_page} from \code{\link[=pdf_load_page]{pdf_load_page()}}, or a \code{pdfium_doc}.}

\item{page}{One-based page index. Only used when \code{x} is a \code{pdfium_doc}.
Ignored otherwise.}
}
\value{
A named numeric vector with elements \code{width} and \code{height}.
}
\description{
Returns the width and height of \code{page} in PDF points (1 point = 1/72 inch).
Accepts either a \code{pdfium_page} (preferred when you already have one) or
a \verb{(doc, page)} pair (convenience for one-shot inspection).
}
\examples{
fixture <- system.file("extdata", "fixtures", "minimal.pdf",
                       package = "pdfium")
if (nzchar(fixture)) {
  doc <- pdf_open(fixture)
  pdf_page_size(doc, 1)
  pdf_close(doc)
}
}
