% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/images.R
\name{pdf_image_data}
\alias{pdf_image_data}
\title{Raw bytes of an embedded image stream}
\usage{
pdf_image_data(obj, decoded = TRUE)
}
\arguments{
\item{obj}{A \code{pdfium_obj} of type \code{"image"}.}

\item{decoded}{Logical scalar. \code{TRUE} (default) returns the
decompressed pixel data after applying all filters; \code{FALSE}
returns the stream bytes as stored.}
}
\value{
A raw vector. Length is whatever PDFium reports - the
filter-applied size for \code{decoded = TRUE}, the stored byte
count for \code{decoded = FALSE}.
}
\description{
Returns the bytes that back the image object - either the
uncompressed pixel buffer (\code{decoded = TRUE}) or the raw embedded
stream as it sits in the PDF (\code{decoded = FALSE}). The raw form is
useful when you want to write the original JPEG / JBIG2 / JPEG2000
/ Flate-deflated bitmap to disk without re-encoding; pair it with
\code{\link[=pdf_image_filters]{pdf_image_filters()}} to know which decoders the PDF specifies.
}
\details{
Wraps \code{FPDFImageObj_GetImageDataDecoded} or
\code{FPDFImageObj_GetImageDataRaw} (chosen by \code{decoded}).
}
\seealso{
\code{\link[=pdf_image_filters]{pdf_image_filters()}}, \code{\link[=pdf_image_bitmap]{pdf_image_bitmap()}}.
}
