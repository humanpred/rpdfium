% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/paths.R
\name{pdf_path_fill}
\alias{pdf_path_fill}
\title{Fill color of a path page-object}
\usage{
pdf_path_fill(obj)
}
\arguments{
\item{obj}{A \code{pdfium_obj} of type \code{"path"} (from
\code{\link[=pdf_page_objects]{pdf_page_objects()}}).}
}
\value{
A named numeric vector \code{c(red, green, blue, alpha)} of
0-255 channel values, or all-\code{NA} when no fill is set.
}
\description{
Returns the RGBA fill color of \code{obj}. Channels are integers in
\verb{[0, 255]}. When PDFium reports that the object has no fill set
(e.g. a stroke-only path), all four channels are \code{NA}.
}
\examples{
fixture <- system.file("extdata", "fixtures", "shapes.pdf",
  package = "pdfium"
)
if (nzchar(fixture)) {
  doc <- pdf_doc_open(fixture)
  p <- pdf_page_load(doc, 1)
  path_obj <- Filter(\(o) o$type == "path", pdf_page_objects(p))[[1]]
  pdf_path_fill(path_obj)
  pdf_page_close(p)
  pdf_doc_close(doc)
}
}
\seealso{
\code{\link[=pdf_path_stroke]{pdf_path_stroke()}}, \code{\link[=pdf_path_segments]{pdf_path_segments()}}
}
