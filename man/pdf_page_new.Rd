% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mutation.R
\name{pdf_page_new}
\alias{pdf_page_new}
\title{Add a new blank page}
\usage{
pdf_page_new(doc, page_num, width, height)
}
\arguments{
\item{doc}{A read-write \code{pdfium_doc}.}

\item{page_num}{Insertion index, 1-based. Must satisfy
\verb{1 <= page_num <= pdf_page_count(doc) + 1}.}

\item{width, height}{Page size in PDF points (1 pt = 1/72 in).
US Letter is \verb{612, 792}; A4 is \verb{595, 842}.}
}
\value{
A \code{pdfium_page} handle for the new page. (Unlike most
mutators this returns a page rather than the doc, because
callers typically want to add content to the page
immediately.)
}
\description{
Wraps \code{FPDFPage_New}. Inserts a new blank page of the given
dimensions at \code{page_num} (1-based). Existing pages at or above
\code{page_num} shift down by one.
}
