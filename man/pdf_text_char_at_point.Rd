% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/page_extras.R
\name{pdf_text_char_at_point}
\alias{pdf_text_char_at_point}
\title{Locate the character index nearest a (x, y) point on a page}
\usage{
pdf_text_char_at_point(page, x, y, tolerance = 2, page_num = 1L)
}
\arguments{
\item{page}{A \code{pdfium_page} from \code{\link[=pdf_load_page]{pdf_load_page()}}, or a
\code{pdfium_doc}.}

\item{x, y}{Point in PDF user-space points.}

\item{tolerance}{Numeric of length 1 or 2; absolute slack
(in PDF points) PDFium is allowed to use when no character
directly contains \verb{(x, y)}. Length-2 sets \code{x} and \code{y} tolerance
independently. Default \code{2}.}

\item{page_num}{One-based page index. Only used when \code{page} is
a \code{pdfium_doc}. Ignored otherwise.}
}
\value{
Integer scalar — the 1-based character index, or \code{NA} when
no character is within tolerance.
}
\description{
Returns the 1-based index of the character whose bounding box
contains (or is closest within \code{tolerance}) the given point. Wraps
\code{FPDFText_GetCharIndexAtPos}.
}
\seealso{
\code{\link[=pdf_text_chars]{pdf_text_chars()}}.
}
