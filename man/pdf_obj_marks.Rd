% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/obj_extras.R
\name{pdf_obj_marks}
\alias{pdf_obj_marks}
\title{Content marks attached to a page object}
\usage{
pdf_obj_marks(obj)
}
\arguments{
\item{obj}{A \code{pdfium_obj} from \code{\link[=pdf_page_objects]{pdf_page_objects()}}.}
}
\value{
A tibble with columns:
\itemize{
\item \code{mark_index} integer - 1-based position in the object's mark
stack.
\item \code{name} character - the mark name (BDC tag).
\item \code{params} list-column - a named list of the mark's parameter
values. Values are typed in R: numeric for \code{FPDF_OBJECT_NUMBER},
character for \verb{_STRING} / \verb{_NAME}, raw vectors for blobs.
}

Returns a 0-row tibble of the same schema when the object has no
marks (typical for content from untagged PDFs).
}
\description{
Returns one tibble row per \emph{content mark} on the page object — the
tagged-PDF mechanism that links a piece of page content (a path,
a text run, an image, ...) to a structure element in
\code{\link[=pdf_structure_tree]{pdf_structure_tree()}}. Wraps \code{FPDFPageObj_CountMarks},
\code{FPDFPageObj_GetMark}, \code{FPDFPageObjMark_GetName},
\verb{_CountParams}, \verb{_GetParamKey}, \verb{_GetParamValueType}, and the
\verb{_GetParamIntValue} / \verb{_GetParamStringValue} /
\verb{_GetParamBlobValue} accessors.
}
\details{
Each mark carries a \emph{name} (typically the structural type or BDC
tag — e.g. \code{"P"}, \code{"Span"}, \code{"Artifact"}) and zero or more
parameters as key/value pairs. The most common parameter is
\code{MCID} (an integer linking the object to a structure tree
element's marked-content reference).
}
\seealso{
\code{\link[=pdf_structure_tree]{pdf_structure_tree()}} for the structure-tree side of
the same linkage; \code{\link[=pdf_obj_type]{pdf_obj_type()}}.
}
