% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/attachment_authoring.R
\name{pdf_attachment_new}
\alias{pdf_attachment_new}
\title{Add a new embedded file attachment to a document}
\usage{
pdf_attachment_new(doc, name)
}
\arguments{
\item{doc}{A \code{pdfium_doc} from \code{\link[=pdf_doc_open]{pdf_doc_open()}} or
\code{\link[=pdf_doc_new]{pdf_doc_new()}}. Must be readwrite.}

\item{name}{Character scalar — the attachment's filename. UTF-8
accepted.}
}
\value{
A \code{pdfium_attachment} handle. The attachment is empty —
call \code{\link[=pdf_attachment_set_data]{pdf_attachment_set_data()}} to populate its contents.
}
\description{
Creates a new \verb{/EmbeddedFile} entry in \code{doc}'s name tree, with the
given filename. The returned handle is a \code{pdfium_attachment} that
you can pass to \code{\link[=pdf_attachment_set_data]{pdf_attachment_set_data()}} to populate the file
bytes, and \code{\link[=pdf_attachment_set_dict_value]{pdf_attachment_set_dict_value()}} to populate dictionary
metadata (\code{"Subtype"}, \code{"Desc"}, etc.).
}
\details{
Wraps \code{FPDFDoc_AddAttachment}. The new attachment is appended to
the end of the document's existing attachment list, and its
\verb{$index} field reflects the resulting 1-based index. PDFium will
refuse the creation (returning \code{NULL}, which we surface as an R
error) if:
\itemize{
\item \code{name} is empty;
\item \code{name} is the name of an existing embedded file in \code{doc};
\item the document's name tree is at its depth limit.
}
}
\seealso{
\code{\link[=pdf_attachments]{pdf_attachments()}} for the read side,
\code{\link[=pdf_attachment_delete]{pdf_attachment_delete()}} to remove an attachment.
}
