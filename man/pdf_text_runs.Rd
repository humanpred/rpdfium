% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/text.R
\name{pdf_text_runs}
\alias{pdf_text_runs}
\title{Extract every text run on a page}
\usage{
pdf_text_runs(page, page_num = 1L)
}
\arguments{
\item{page}{A \code{pdfium_page} from \code{\link[=pdf_load_page]{pdf_load_page()}}, or a
\code{pdfium_doc} (in which case the first page is loaded and
closed automatically).}

\item{page_num}{One-based page index. Only used when \code{page} is a
\code{pdfium_doc}. Ignored otherwise.}
}
\value{
A tibble with columns:
\itemize{
\item \code{obj_index} - 1-based page-object index (so this row is the
\code{obj_index}-th object returned by \code{\link[=pdf_page_objects]{pdf_page_objects()}}).
Renamed from \code{text_index} in the v0.1.0 reader/writer audit
to avoid colliding with \code{pdf_text_chars()$text_index}, which
is the \emph{extractable-text} offset.
\item \code{bounds_left}, \code{bounds_bottom}, \code{bounds_right}, \code{bounds_top}
\itemize{
\item the object's bounding box in PDF points
}
\item \code{font_size} - typographic em size; multiply by the text
object's matrix scale (when available) for rendered size
\item \code{text} - UTF-8 string
}
}
\description{
Returns one row per text page-object on \code{page}, with the text
content, bounding box, font size, and 1-based page-object index.
Loads PDFium's per-page text-extraction context
(\code{FPDFText_LoadPage}) once and reuses it across every text
object on the page; this is materially faster than calling
\code{\link[=pdf_text_content]{pdf_text_content()}} in a loop, which opens and closes a text
page per object.
}
\details{
The returned tibble's schema matches the \code{text_runs} attribute
produced by \code{\link[=pdf_extract_paths]{pdf_extract_paths()}}.
}
\examples{
fixture <- system.file("extdata", "fixtures", "unicode.pdf",
                       package = "pdfium")
if (nzchar(fixture)) {
  doc <- pdf_open(fixture)
  pdf_text_runs(doc, 1)
  pdf_close(doc)
}
}
\seealso{
\code{\link[=pdf_text_content]{pdf_text_content()}}, \code{\link[=pdf_extract_paths]{pdf_extract_paths()}}
}
