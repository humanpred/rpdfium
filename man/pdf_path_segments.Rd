% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/paths.R
\name{pdf_path_segments}
\alias{pdf_path_segments}
\title{Path segments of a path page-object}
\usage{
pdf_path_segments(obj)
}
\arguments{
\item{obj}{A \code{pdfium_obj} of type \code{"path"} (from
\code{\link[=pdf_page_objects]{pdf_page_objects()}}).}
}
\value{
A tibble with the columns described above. An empty path
returns a 0-row tibble of the same shape.
}
\description{
Returns one row per segment of the path. Segments are emitted in
the same order they appear in the page's content stream, which is
the same order PDFium's rendering pipeline consumes. The result is
suitable for plotting the geometry or for downstream coordinate
analysis.
}
\details{
Each row carries:
\itemize{
\item \code{index} - 1-based segment index within this path
\item \code{type} - \code{"moveto"}, \code{"lineto"}, \code{"bezierto"}, or \code{"unknown"}
\item \code{x}, \code{y} - the segment's anchor point in PDF points
\item \code{close} - \code{TRUE} if this segment closes the current subpath
(PDFium's \code{h} operator equivalent)
}

\strong{Known limitation:} PDFium's segment readout API exposes only the
endpoint of a \code{bezierto} segment, not its two control points.
Recovering control points requires content-stream parsing and is
deferred. For now, \code{bezierto} rows show the curve's endpoint; the
control-point information is lost. See
\code{docs/pdfium-api-review.md} for the full discussion.
}
\examples{
fixture <- system.file("extdata", "fixtures", "shapes.pdf",
                       package = "pdfium")
if (nzchar(fixture)) {
  doc <- pdf_open(fixture)
  p <- pdf_load_page(doc, 1)
  path_obj <- Filter(\(o) o$type == "path", pdf_page_objects(p))[[1]]
  pdf_path_segments(path_obj)
  pdf_close_page(p)
  pdf_close(doc)
}
}
\seealso{
\code{\link[=pdf_page_objects]{pdf_page_objects()}}, \code{\link[=pdf_obj_bounds]{pdf_obj_bounds()}}
}
