% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/struct_tree.R
\name{pdf_structure_tree}
\alias{pdf_structure_tree}
\title{Read the tagged-PDF structure tree for a page}
\usage{
pdf_structure_tree(page, page_num = 1L)
}
\arguments{
\item{page}{A \code{pdfium_page} from \code{\link[=pdf_load_page]{pdf_load_page()}}, or a
\code{pdfium_doc}.}

\item{page_num}{One-based page index. Only used when \code{page} is
a \code{pdfium_doc}. Ignored otherwise.}
}
\value{
A tibble with columns:
\itemize{
\item \code{element_index} integer - 1-based pre-order position in the
page's tree walk.
\item \code{parent_index} integer - the \code{element_index} of the parent
element; \code{0} for top-level entries (children of the page's
structure-tree root).
\item \code{level} integer - 1-based nesting depth.
\item \code{type} character - the structural element type (\verb{/S}),
UTF-8. Common values follow the PDF spec's standard
structure types (e.g. \code{"Document"}, \code{"Sect"}, \code{"P"},
\code{"H1"}, \code{"Span"}, \code{"Figure"}, \code{"Table"}).
\item \code{title} character - the element's \verb{/T} title (often empty).
\item \code{lang} character - the element's \verb{/Lang} IETF BCP 47 code
(e.g. \code{"en"}, \code{"fr"}); empty when none is set.
\item \code{alt_text} character - the element's \verb{/Alt} alternative
text for assistive technology; empty when none is set.
\item \code{actual_text} character - the element's \verb{/ActualText}
replacement text; empty when none is set.
\item \code{id} character - the element's \verb{/ID} string (often empty).
\item \code{mcid} integer - the first marked-content ID associated
with the element (whether direct \verb{/K N} or via the first
\verb{/MCR} child); \code{NA} when the element has no marked
content of its own (typical for container elements like
\code{Document} / \code{Sect}).
\item \code{mcid_count} integer - how many marked-content IDs the
element references; \code{0} for elements without content,
\code{1} for the simple \verb{/K N} case, \verb{>1} for elements that
span several content tags.
\item \code{obj_type} character - the element's \verb{/Type} entry
(typically \code{"StructElem"}; empty when not set).
\item \code{attributes} list-column - a named list of the element's
structural attributes (PDF spec table 354+), with R-typed
values: logical for \verb{/O /Layout /BBox}-like booleans,
numeric for \verb{/RowSpan} / \verb{/ColSpan} / \verb{/StartIndent},
character for \verb{/Placement} / \verb{/TextAlign} /
\verb{/Lang}-style names. Empty list when the element has no
\verb{/A} attribute objects. Aggregated across all attribute
dictionaries on the element (PDF's nested attribute-class
layout is flattened to a single namespace).
}

Returns a 0-row tibble of the same schema when the page has
no associated structure tree (typical for untagged PDFs).
}
\description{
Returns one tibble row per accessibility structure element
associated with the given page, walking PDFium's view of the
document's \verb{/StructTreeRoot} depth-first. Each row carries the
element's structural type (e.g. \code{"P"}, \code{"H1"}, \code{"Span"},
\code{"Figure"}), its title / language / alternative text, the
marked-content ID linking it to a page-content tag, and the
tree shape (parent_index + level).
}
\details{
Most PDFs are not tagged; for those, this function returns a
0-row tibble. Tagging is required for \code{print_high_res}-quality
accessibility, screen-reader support, and PDF/UA conformance.

Wraps \code{FPDF_StructTree_GetForPage}, \code{FPDF_StructTree_*Children},
\code{FPDF_StructElement_GetType} / \code{GetTitle} / \code{GetLang} /
\code{GetAltText} / \code{GetActualText} / \code{GetID} /
\code{GetMarkedContentID}.
}
\seealso{
\code{\link[=pdf_doc_is_tagged]{pdf_doc_is_tagged()}} for a fast yes/no check at the
document level.
}
