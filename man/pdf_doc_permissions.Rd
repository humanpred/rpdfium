% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/doc.R
\name{pdf_doc_permissions}
\alias{pdf_doc_permissions}
\title{Permission flags from a PDF's encryption dictionary}
\usage{
pdf_doc_permissions(doc)
}
\arguments{
\item{doc}{A \code{pdfium_doc} from \code{\link[=pdf_doc_open]{pdf_doc_open()}}, or a character path.}
}
\value{
A named logical vector with the eight flags listed above.
}
\description{
Returns the operations the PDF declares it allows. When the
document is unencrypted (or was opened with the owner password),
PDFium reports \code{0xFFFFFFFF} - every bit set, every operation
allowed - and this function returns a named logical vector of
all \code{TRUE}. For an encrypted document opened with a user
password, the bitmask reflects whatever the document author set.
}
\details{
Wraps \code{FPDF_GetDocPermissions}. The decoded flags follow the PDF
specification's \verb{/P} (UserAccess) bit assignments (ISO 32000-1
section 7.6.3.2, Table 22):
\itemize{
\item \code{print} - bit 3: print the document.
\item \code{modify} - bit 4: change content other than annotation /
form-field values.
\item \code{copy} - bit 5: copy or otherwise extract text and graphics
from the document.
\item \code{annotate} - bit 6: add or modify text annotations.
\item \code{fill_forms} - bit 9: fill in interactive form fields,
regardless of \code{modify}.
\item \code{extract_for_a11y} - bit 10: extract text and graphics for
accessibility purposes.
\item \code{assemble} - bit 11: insert, rotate, or delete pages and
create bookmarks / thumbnails, regardless of \code{modify}.
\item \code{print_high_res} - bit 12: faithful digital print copy. When
\code{FALSE} while \code{print} is \code{TRUE}, the document may print only
at low resolution.
}
}
