% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/attachment_authoring.R
\name{pdf_attachment_delete}
\alias{pdf_attachment_delete}
\title{Delete an embedded file attachment from a document}
\usage{
pdf_attachment_delete(att)
}
\arguments{
\item{att}{A \code{pdfium_attachment} from \code{\link[=pdf_attachments]{pdf_attachments()}} or
\code{\link[=pdf_attachment_new]{pdf_attachment_new()}}. Parent doc must be readwrite.}
}
\value{
Invisibly returns the parent \code{pdfium_doc}.
}
\description{
Removes the attachment's entry from the document's \verb{/EmbeddedFiles}
name tree. Wraps \code{FPDFDoc_DeleteAttachment}. The handle becomes
closed (subsequent reads / writes through it error cleanly);
indexes of any later attachments shift down by one in PDFium's
internal list — re-fetch via \code{\link[=pdf_attachments]{pdf_attachments()}} if you held
other handles past this point.
}
\details{
Note: PDFium's delete only removes the name-tree pointer; the
underlying \verb{/EmbeddedFile} object may still occupy bytes in the
saved PDF. This matches \code{FPDFDoc_DeleteAttachment}'s documented
behaviour.
}
\seealso{
\code{\link[=pdf_attachment_new]{pdf_attachment_new()}}.
}
