% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/render.R
\name{pdf_render_to_png}
\alias{pdf_render_to_png}
\title{Render a PDF page directly to a PNG file}
\usage{
pdf_render_to_png(
  page,
  file,
  page_num = 1L,
  dpi = 72,
  background = "white",
  annotations = FALSE,
  rotation = 0L
)
}
\arguments{
\item{page}{A \code{pdfium_page} from \code{\link[=pdf_load_page]{pdf_load_page()}}, or a
\code{pdfium_doc} (the page given by \code{page_num} will be loaded and
closed internally).}

\item{file}{Output file path.}

\item{page_num}{One-based page index. Only used when \code{page} is a
\code{pdfium_doc}. Ignored otherwise.}

\item{dpi}{Render resolution in dots per inch (default \code{72},
meaning one pixel per PDF point). Higher values give larger,
sharper output at proportional memory cost.}

\item{background}{Background color drawn behind the page content
before rendering. Accepts any string \code{\link[grDevices:col2rgb]{grDevices::col2rgb()}}
understands (named color, \code{"#RRGGBB"}, \code{"#RRGGBBAA"}), or \code{NA}
for a fully transparent background. Defaults to \code{"white"}.}

\item{annotations}{Logical; render annotation appearance streams
on top of the page content. Defaults to \code{FALSE}.}

\item{rotation}{Extra rotation in degrees applied on top of the
page's own \verb{/Rotate} attribute. One of \code{0}, \code{90}, \code{180}, \code{270}.
Note: PDFium's rotation is clockwise; e.g. \code{90} means rotate
the page 90° clockwise from its on-page orientation.}
}
\value{
Invisibly returns \code{file}.
}
\description{
Convenience wrapper that calls \code{\link[=pdf_render_page]{pdf_render_page()}} and writes the
resulting bitmap to disk through R's built-in PNG graphics device.
Uses \code{\link[grDevices:png]{grDevices::png()}} for the file output and
\code{\link[grid:grid.raster]{grid::grid.raster()}} for the pixel placement, so it depends only
on R's recommended packages — no extra runtime dependency.
}
\details{
The PNG device is sized to exactly match the rendered bitmap so
\code{\link[grid:grid.raster]{grid::grid.raster()}} (\code{interpolate = FALSE}) writes one PDFium
pixel per PNG pixel. The output is RGBA when any pixel has alpha
below 1 and RGB otherwise (R's PNG device flattens the alpha
channel away when it's uniformly opaque, which is a lossless
equivalence — the on-disk bytes will not match
\code{png::writePNG()} byte-for-byte but the rendered image will).
}
\examples{
fixture <- system.file("extdata", "fixtures", "shapes.pdf",
                       package = "pdfium")
if (nzchar(fixture)) {
  out <- tempfile(fileext = ".png")
  pdf_render_to_png(pdf_open(fixture), file = out, dpi = 96)
  file.exists(out)
}
}
