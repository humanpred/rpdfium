% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/page_extras.R
\name{pdf_page_box}
\alias{pdf_page_box}
\title{Read a page's bounding box}
\usage{
pdf_page_box(
  page,
  page_num = 1L,
  box = c("media", "crop", "bleed", "trim", "art")
)
}
\arguments{
\item{page}{A \code{pdfium_page} from \code{\link[=pdf_load_page]{pdf_load_page()}}, or a
\code{pdfium_doc}.}

\item{page_num}{One-based page index. Only used when \code{page} is
a \code{pdfium_doc}. Ignored otherwise.}

\item{box}{One of \code{"media"} (default), \code{"crop"}, \code{"bleed"},
\code{"trim"}, \code{"art"}.}
}
\value{
A named numeric vector with elements \code{left}, \code{bottom},
\code{right}, \code{top} (PDF user-space points). When the requested
box is not declared in the PDF, PDFium falls back per the
spec (e.g. CropBox -> MediaBox if absent); when no fallback
exists, every element is \code{NA}.
}
\description{
PDF pages can carry up to five named boxes:
\href{https://www.iso.org/standard/63534.html}{MediaBox} (physical
page extent), CropBox (visible / printable extent), BleedBox
(printer trim with bleed), TrimBox (final page after cutting),
and ArtBox (meaningful content). \code{\link[=pdf_page_size]{pdf_page_size()}} returns the
MediaBox's width/height; this function returns any of the five
boxes as a \verb{(left, bottom, right, top)} named vector.
}
\details{
Wraps \code{FPDFPage_GetMediaBox} / \verb{_GetCropBox} / \verb{_GetBleedBox}
/ \verb{_GetTrimBox} / \verb{_GetArtBox}.
}
\seealso{
\code{\link[=pdf_page_size]{pdf_page_size()}} (always MediaBox width/height).
}
