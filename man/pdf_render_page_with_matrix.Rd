% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/render.R
\name{pdf_render_page_with_matrix}
\alias{pdf_render_page_with_matrix}
\title{Render a PDF page with an arbitrary affine transformation}
\usage{
pdf_render_page_with_matrix(
  page,
  matrix,
  pixel_width,
  pixel_height,
  clip_rect = NULL,
  page_num = 1L,
  background = "white",
  annotations = FALSE
)
}
\arguments{
\item{page}{A \code{pdfium_page} from \code{\link[=pdf_page_load]{pdf_page_load()}}, or a
\code{pdfium_doc} (the page given by \code{page_num} will be loaded and
closed internally).}

\item{matrix}{Length-6 numeric vector (or 3x2 / 2x3 matrix
coerced to length-6).}

\item{pixel_width, pixel_height}{Output bitmap dimensions in
pixels (positive integers).}

\item{clip_rect}{Length-4 numeric \code{c(left, bottom, right, top)}
in PDF user-space points, or \code{NULL} to skip clipping.}

\item{page_num}{One-based page index. Only used when \code{page} is a
\code{pdfium_doc}. Ignored otherwise.}

\item{background}{Background color drawn behind the page content
before rendering. Accepts any string \code{\link[grDevices:col2rgb]{grDevices::col2rgb()}}
understands (named color, \code{"#RRGGBB"}, \code{"#RRGGBBAA"}), or \code{NA}
for a fully transparent background. Defaults to \code{"white"}.}

\item{annotations}{Logical; render annotation appearance streams
on top of the page content. Defaults to \code{FALSE}.}
}
\value{
A \code{pdfium_bitmap}.
}
\description{
Power-user counterpart to \code{\link[=pdf_render_page]{pdf_render_page()}}. Instead of
choosing a DPI + rotation, the caller supplies a 3x2 affine
transformation matrix and the destination bitmap's pixel
dimensions, plus an optional clipping rectangle in PDF
user-space points. Useful for:
}
\details{
\itemize{
\item Rendering a cropped region of a page (set the matrix to scale
\itemize{
\item translate the desired region into the bitmap, plus a
matching \code{clip_rect} to discard everything outside).
}
\item Implementing zoom / pan in interactive viewers.
\item Pre-warping for non-rectilinear projections (the matrix can
include shear).
}

Wraps \code{FPDF_RenderPageBitmapWithMatrix}.
}
\section{Matrix layout}{


\code{matrix} is a 3x2 numeric matrix (or a length-6 numeric vector)
representing the PDFium-order affine transformation
\verb{(a, b, c, d, e, f)}, applied as:

\deqn{x' = a\cdot x + c\cdot y + e}
\deqn{y' = b\cdot x + d\cdot y + f}

For a simple scale, use \code{matrix(c(s, 0, 0, s, 0, 0), 3, 2, byrow = TRUE)}. To crop to \verb{(x0, y0)} -> \verb{(x1, y1)} at scale
\code{s}, use \code{matrix(c(s, 0, 0, s, -s*x0, -s*y0), 3, 2, byrow = TRUE)} plus \code{clip_rect = c(x0, y0, x1, y1)}.
}

\seealso{
\code{\link[=pdf_render_page]{pdf_render_page()}} for the simpler dpi+rotation API.
}
