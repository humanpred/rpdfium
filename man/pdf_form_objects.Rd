% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/forms.R
\name{pdf_form_objects}
\alias{pdf_form_objects}
\title{List the page objects nested inside a Form XObject}
\usage{
pdf_form_objects(form)
}
\arguments{
\item{form}{A \code{pdfium_obj} of type \code{"form"}, typically obtained
by filtering \code{\link[=pdf_page_objects]{pdf_page_objects()}} (or another
\code{pdf_form_objects()} call) on \code{type == "form"}.}
}
\value{
A list of \code{pdfium_obj}s, one per nested page object.
Empty list when the form has no children.
}
\description{
Wraps \code{FPDFFormObj_CountObjects} + \code{FPDFFormObj_GetObject} to
enumerate the page objects contained in a Form XObject. The
returned objects participate in the regular \code{pdfium_obj} API -
you can call \code{\link[=pdf_obj_type]{pdf_obj_type()}}, \code{\link[=pdf_obj_bounds]{pdf_obj_bounds()}},
\code{\link[=pdf_obj_matrix]{pdf_obj_matrix()}}, and (per object type)
\code{\link[=pdf_path_segments]{pdf_path_segments()}} / \code{\link[=pdf_image_info]{pdf_image_info()}} / \code{\link[=pdf_text_content]{pdf_text_content()}}
on each one. Nesting is recursive: a Form XObject may itself
contain other Form XObjects, and the returned \code{pdfium_obj}s of
type \code{"form"} can be passed back into \code{pdf_form_objects()}.
}
\details{
Each returned object carries a \code{parent_form} slot pointing back
at \code{form}, used by the print/format methods to show the
containment path (\code{"obj 2 of form 1 on page 1"}). Lifetime is
bound to the parent page, not to the form: as long as the page
is open, the form and its nested objects remain valid.
}
\examples{
fixture <- system.file("extdata", "fixtures", "form_xobject.pdf",
  package = "pdfium"
)
if (nzchar(fixture)) {
  doc <- pdf_doc_open(fixture)
  page <- pdf_page_load(doc, 1L)
  forms <- Filter(function(o) o$type == "form", pdf_page_objects(page))
  if (length(forms) > 0L) {
    nested <- pdf_form_objects(forms[[1L]])
    length(nested)
  }
  pdf_page_close(page)
  pdf_doc_close(doc)
}
}
\seealso{
\code{\link[=pdf_page_objects]{pdf_page_objects()}} for the top-level enumeration,
\code{\link[=pdf_obj_matrix]{pdf_obj_matrix()}} for the form's own transformation matrix.
}
