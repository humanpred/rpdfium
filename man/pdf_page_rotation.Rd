% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/page.R
\name{pdf_page_rotation}
\alias{pdf_page_rotation}
\title{Page rotation in degrees}
\usage{
pdf_page_rotation(page, page_num = 1L)
}
\arguments{
\item{page}{A \code{pdfium_page} from \code{\link[=pdf_load_page]{pdf_load_page()}}, or a
\code{pdfium_doc}.}

\item{page_num}{One-based page index. Only used when \code{page} is a
\code{pdfium_doc}. Ignored otherwise.}
}
\value{
An integer in \verb{\{0, 90, 180, 270\}}.
}
\description{
Returns the page's rotation as \code{0}, \code{90}, \code{180}, or \code{270} degrees.
PDFium reports the rotation stored in the page's \verb{/Rotate} entry
combined with any runtime rotation applied via the editing API.
}
\details{
A non-zero rotation means \code{\link[=pdf_page_size]{pdf_page_size()}}'s \code{width} and \code{height}
refer to the page's pre-rotation media box, not the on-screen
dimensions a viewer would display. For an "as-displayed" size, swap
\code{width} and \code{height} when rotation is \code{90} or \code{270}.
}
\examples{
fixture <- system.file("extdata", "fixtures", "minimal.pdf",
  package = "pdfium"
)
if (nzchar(fixture)) {
  doc <- pdf_open(fixture)
  pdf_page_rotation(doc, 1)
  pdf_close(doc)
}
}
\seealso{
\code{\link[=pdf_page_size]{pdf_page_size()}} for the un-rotated dimensions.
}
