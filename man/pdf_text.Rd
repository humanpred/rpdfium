% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/doc_extra.R
\name{pdf_text}
\alias{pdf_text}
\title{Read every page's text in one call}
\usage{
pdf_text(doc, password = NULL)
}
\arguments{
\item{doc}{A \code{pdfium_doc} from \code{\link[=pdf_open]{pdf_open()}}, or a character path.}

\item{password}{Optional password for encrypted PDFs when \code{doc}
is a path. Ignored when \code{doc} is already an open \code{pdfium_doc}.}
}
\value{
Character vector of length \code{pdf_page_count(doc)}. Each
element is UTF-8 encoded.
}
\description{
Convenience wrapper that returns the document's text content
one string per page, matching the shape of
\code{pdftools::pdf_text()}. Each element is the concatenated text
of every text run on the corresponding page, joined with \code{"\\n"}
between runs.
}
\details{
Internally walks the document with \code{\link[=pdf_text_runs]{pdf_text_runs()}} to reuse
the batched text-page load.
}
\examples{
fixture <- system.file("extdata", "fixtures", "shapes.pdf",
                       package = "pdfium")
if (nzchar(fixture)) pdf_text(fixture)
}
\seealso{
\code{\link[=pdf_text_runs]{pdf_text_runs()}} for run-level structure (font,
bounding box).
}
