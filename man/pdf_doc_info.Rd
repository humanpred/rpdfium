% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/document.R
\name{pdf_doc_info}
\alias{pdf_doc_info}
\title{Document-level metadata for a PDF}
\usage{
pdf_doc_info(doc)
}
\arguments{
\item{doc}{A \code{pdfium_doc} from \code{\link[=pdf_open]{pdf_open()}}, or a character path.}
}
\value{
A list with elements:
\itemize{
\item \code{page_count} - integer
\item \code{file_version} - integer; PDFium reports \code{10 * major + minor}
(e.g. \code{17} for PDF 1.7)
\item \code{title}, \code{author}, \code{subject}, \code{keywords}, \code{creator},
\code{producer}, \code{creation_date}, \code{mod_date}, \code{trapped} - character
\item \code{creation_date_parsed}, \code{mod_date_parsed} - POSIXct (UTC),
\code{NA} when the source date is empty or unparseable
}
}
\description{
Returns the page count, the PDF file version, every standard
Info-dictionary entry, and POSIXct parses of the two date
fields. The shape mirrors \code{pdftools::pdf_info()} to ease
porting.
}
\details{
Standard Info-dictionary entries are UTF-8 strings; missing
entries appear as \code{""}. Date strings come back in the PDF format
\code{"D:YYYYMMDDHHmmSS+HH'mm'"} and are also parsed into POSIXct
(UTC) in the \code{creation_date_parsed} and \code{mod_date_parsed}
slots; parses that fail return \code{NA}.
}
\examples{
fixture <- system.file("extdata", "fixtures", "shapes.pdf",
                       package = "pdfium")
if (nzchar(fixture)) {
  info <- pdf_doc_info(fixture)
  info$page_count
  info$producer
  info$creation_date_parsed
}
}
\seealso{
\code{\link[=pdf_doc_meta]{pdf_doc_meta()}} for arbitrary tag access,
\code{\link[=pdf_parse_date]{pdf_parse_date()}} for the date-parser used internally.
}
